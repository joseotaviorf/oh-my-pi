---
alwaysApply: false
globs: "**/*.sql"
---
# SQL Style Guide

## General SQL Principles

- **Enforce consistency**: All SQL code should follow these guidelines
- **Prioritize readability**: Code should be easy to read and understand
- **Use explicit syntax**: Always use explicit keywords (e.g., `AS` for aliases)
- **Avoid ambiguity**: Use descriptive names and avoid generic aliases

---

## 1. Column Naming and Arrangement

### Naming Conventions

- **Language**: All columns must be in English
- **Case**: Use lowercase, except for reserved words and functions (UPPERCASE)
- **Format**: Use snake_case (e.g., `column_name`)
- **Aliases**: Avoid generic aliases (e.g., `table_name AS t`)
- **Reserved words**: Avoid using reserved words as column names (e.g., `type`, `date`)
- **IDs**: Avoid ambiguous `id` - add context (e.g., `id_user`, `id_order` instead of just `id`)
- **Source-to-model alias normalization**: When source names are identifiable, normalize with explicit aliases:
  - IDs: suffix-style to prefix-style (e.g., `client_id AS id_client`, `usuario_id AS id_user`)
  - Timestamps: `ts_<past_tense_when_possible>` (e.g., `created_at AS ts_created`)
  - Dates: `dt_<past_tense_when_possible>` (e.g., `signed_date AS dt_signed`)
  - If past tense is not natural/clear, use semantic fallback (e.g., `contract_date AS dt_contract`)

### Column Arrangement Priority

Follow column arrangement priority order: IDs → UUIDs → Non-SKs → Characteristics → Metrics → Booleans → Dates → Timestamps → Partitions. See `naming_conventions.mdc` for full definitions and examples.

---

## 2. Aliasing and Correlations

### Rules

- **Avoid generic aliases**: Do not use single letters or generic names (e.g., `table_name AS t`)
- **Use descriptive aliases**: Aliases should relate to the object/expression they represent
- **Avoid full names**: Don't use the whole table name as alias (becomes cluttered)
- **Always use AS**: Always include the `AS` keyword for explicit readability

**Good Example:**

```sql
SELECT
    tn1.id AS id_table,
    DATE(tn2.submission_timestamp) AS day
FROM
    table_name_1 AS tn1
JOIN
    table_name_2 AS tn2
    ON tn1.id = tn2.id
```

**Bad Example:**

```sql
SELECT
    table_name_1.id id_table,
    DATE(table_name_2.submission_timestamp) day
FROM
    table_name_1
JOIN
    table_name_2
    ON table_name_1.id = table_name_2.id
```

---

## 3. Formatting Rules

- **UPPERCASE** all reserved keywords and functions (`SELECT`, `FROM`, `WHERE`, `JOIN`, `AS`, `COUNT()`, `CASE WHEN`, `AND`, `OR`, `ON`, `IN`, `IS`, `NOT`, `NULL`)
- **Spaces** before/after operators (`=`, `>`, `<`) and after commas
- **New lines** before `AND`/`OR`/`ON`, after root keywords (`SELECT`/`FROM`/`WHERE`/`JOIN`), and after semicolons. Do not use blank lines (DBeaver incompatible).
- **Indentation**: Keywords left-aligned; columns indented by TAB or 2/4 spaces
- **Commas** at end-of-line (right comma)
- **Each column** on its own row in `SELECT`. Never use `SELECT *`.
- **DISTINCT** on the same row as `SELECT`

```sql
SELECT DISTINCT
    COUNT(*) AS total_count,
    DATE(created_at) AS dt_created
FROM
    users
WHERE
    status = 'active'
    AND created_at > '2024-01-01'
```

---

## 7. CTEs (Common Table Expressions)

### Rules

- **Use CTEs instead of subqueries**: CTEs make SQL more readable and performant
- **Placement**: CTEs should be placed at the top of the query
- **Naming**: CTE names should be concise but clear
- **Avoid generic names**: Do not use generic names like "a", "test", etc.

### Format

```sql
WITH context_1 AS (
    SELECT
        column_1,
        column_2
    FROM
        table_1
),
context_2 AS (
    SELECT
        column_3,
        column_4
    FROM
        table_2
)
SELECT
    c1.column_1,
    c2.column_3
FROM
    context_1 AS c1
JOIN
    context_2 AS c2
    ON c1.column_1 = c2.column_3
```

---

## 8. Joins

### Rules

- **Root keys**: Join keywords should have their own line
- **Table names**: Use new lines for table names
- **ON/AND**: Place `ON` and `AND` on new lines an aditional identation
- **Indentation**: Use TAB or 2/4 spaces when moving to new row

**Good Example:**

```sql
SELECT
    tn_1.column_1,
    tn_2.column_2,
    tn_3.column_3
FROM
    table_name_1 AS tn_1
LEFT JOIN
    table_name_2 AS tn_2
        ON tn_1.column_1 = tn_2.column_1
        AND tn_1.column_2 = tn_2.column_2
LEFT JOIN
    table_name_3 AS tn_3
        ON tn_1.column_3 = tn_3.column_3
```

**Bad Example:**

```sql
SELECT
    tn_1.column_1,
    tn_1.column_2,
    tn_2.column_3
FROM table_name_1 AS tn_1
LEFT JOIN 
    table_name_2 AS tn_2 
    ON tn_1.column_1 = tn_2.column_1
    AND tn_1.column_2 = tn_2.column_2
LEFT JOIN table_name_3 AS tn_3 ON tn_1.column_3 = tn_3.column_3
```

---

## 9. Case-When Statements

### Rules

- **Each WHEN**: Should be on a new indented row
- **END AS**: Should be on a new row, indented the same way as `CASE`
- **Nested CASE**: When inside another function, break it as aforementioned but at the new indentation level

**Good Example:**

```sql
SELECT
    CASE column_1
        WHEN 'a'
         OR 'aa' THEN 'first'
        WHEN 'b' THEN 'second'
        ELSE 'third'
    END AS new_column
FROM
    table_1
WHERE
    column_2 IS NOT NULL
```

**Good Example (Nested in Function):**

```sql
SELECT
    COUNT(
        CASE column_1
            WHEN 'b' THEN 1
        END
    ) > 0 AS is_bee
FROM
    table_1
WHERE
    column_2 IS NOT NULL
```

---

## 10. Commenting

### Rules

- **Placement**: Comments should go near the top of your query, or at least near the closest `SELECT`
- **Single line**: Use `--` syntax (necessarily at the beginning of the line)
- **Multi-line**: Use `/* */` syntax, a line above
- **Indentation**: Keep comments indented - avoid too large lines mixed with small ones
- **Length**: If the comment is too long, it probably shouldn't be there, but in the model documentation
- **No blank lines**: Do not use blank lines between comment and query

**Good Example:**

```sql
-- Single line comment goes here
SELECT
    column_1,
    column_2
FROM
    table_name
WHERE
    column_1 IS NOT NULL
```

**Good Example (Multi-line):**

```sql
/* Multi-line comment goes here. Always keep in
mind that it is not cool to have long lines. You should
try to keep them with a similar size, alright? */
SELECT
    column_1,
    column_2
FROM
    table_name
WHERE
    column_1 IS NOT NULL
```

---

## 11. No Wildcard Selects

### Rule

`SELECT *` is **prohibited** in all query files without exception. Every column must be named explicitly. This applies to:

- Top-level `SELECT` statements
- CTEs (`WITH ... AS (SELECT ...)`)
- Subqueries
- `QUALIFY` windows (inline deduplication)

### Rationale

1. **PII leakage**: Wildcard selects silently pull every column in the source table, including columns that contain sensitive personal data (CPF, email, phone, etc.) that were never intended to reach the destination layer.
2. **Column-level lineage breaks**: The governance metadata system derives lineage from explicit column references. `SELECT *` produces no traceable lineage, making it impossible to track where PII flows.
3. **Partition pruning disabled**: Databricks cannot push down predicates through `SELECT *` in intermediate CTEs, which forces full table scans and significantly increases compute cost and run time.
4. **Schema drift**: When a source table gains or removes columns, `SELECT *` silently changes the output schema of downstream tables.

### Good Example

```sql
WITH active_users AS (
    SELECT
        id_user,
        email,
        ts_created
    FROM
        datalake_ebdb_clean.user
    WHERE
        is_active = true
)
SELECT
    au.id_user,
    au.email,
    au.ts_created
FROM
    active_users AS au
QUALIFY ROW_NUMBER() OVER (PARTITION BY au.id_user ORDER BY au.ts_created DESC) = 1
```

### Bad Example

```sql
-- Forbidden: SELECT * in CTE, subquery, and QUALIFY window
WITH active_users AS (
    SELECT *
    FROM
        datalake_ebdb_clean.user
    WHERE
        is_active = true
)
SELECT *
FROM
    active_users
QUALIFY ROW_NUMBER() OVER (PARTITION BY id_user ORDER BY ts_created DESC) = 1
```

---

## 12. Query Performance and Partition Filtering

### Rules

#### 12a — Always filter on partition columns

When reading a partitioned table, include a predicate on its partition columns (`year`, `month`, `day`, or the date column declared as `partitions` in the DAG declaration). Without this, Databricks reads every partition (full table scan).

**Good:**

```sql
SELECT
    id_contract,
    status,
    ts_updated
FROM
    datalake_ebdb_clean.contract
WHERE
    year = YEAR('{load_start_date}')
    AND month = MONTH('{load_start_date}')
    AND day = DAY('{load_start_date}')
```

**Bad:**

```sql
-- No partition filter — reads all partitions
SELECT
    id_contract,
    status,
    ts_updated
FROM
    datalake_ebdb_clean.contract
```

#### 12b — Use incremental load params for large tables

For tables that grow continuously, use the `{load_start_date}` / `{load_end_date}` template params (resolved by the Spark job at runtime) instead of querying all historical data on every run.

**Good:**

```sql
SELECT
    id_event,
    event_type,
    ts_event
FROM
    datalake_amplitude_clean.event
WHERE
    dt_event >= '{load_start_date}'
    AND dt_event < '{load_end_date}'
```

**Bad:**

```sql
-- Full scan every run — never use for large, growing tables
SELECT
    id_event,
    event_type,
    ts_event
FROM
    datalake_amplitude_clean.event
```

#### 12c — Push filters before joins

Apply `WHERE` filters inside a CTE before joining to large tables. Filtering after the join forces Databricks to read and shuffle the full dataset first.

**Good:**

```sql
WITH recent_contracts AS (
    SELECT
        id_contract,
        id_user,
        status
    FROM
        datalake_ebdb_clean.contract
    WHERE
        dt_signed >= '{load_start_date}'
        AND dt_signed < '{load_end_date}'
)
SELECT
    rc.id_contract,
    rc.status,
    du.user_name
FROM
    recent_contracts AS rc
LEFT JOIN
    dw_rent.dim_user AS du
        ON rc.id_user = du.id_user
```

**Bad:**

```sql
-- Filter applied after join — full table scan on both sides
SELECT
    c.id_contract,
    c.status,
    du.user_name
FROM
    datalake_ebdb_clean.contract AS c
LEFT JOIN
    dw_rent.dim_user AS du
        ON c.id_user = du.id_user
WHERE
    c.dt_signed >= '{load_start_date}'
    AND c.dt_signed < '{load_end_date}'
```

#### 12d — Align predicates with Z-order columns

When a table declares `z_order_by` columns in its DAG declaration, include those columns in `WHERE` or `JOIN` predicates whenever the query logic allows it. Z-ordering clusters data by those columns on disk; predicates on those columns skip data files via data skipping statistics.

```sql
-- Z-ordered by id_user and dt_event — both columns used in the WHERE clause
SELECT
    id_event,
    event_type,
    ts_event
FROM
    datalake_amplitude_clean.event
WHERE
    id_user = '{user_id}'
    AND dt_event >= '{load_start_date}'
    AND dt_event < '{load_end_date}'
```

---

## 13. Person Data Model — PII Centralisation

### Rule

The [RFC "Person as PII Data Model at 5A" (Closed Apr 2025)](docs/pii_audit.md) is the authoritative policy for personal data in the analytical environment. Two tables are live in production and must be used as the single source of truth for person identity:

| Table | Layer | Key columns available |
|-------|-------|----------------------|
| `datalake_person.person_sks` | Enrich | `sk_person`, `id_person`, `id_user`, `uuid_person`, `id_contact_info_email`, `id_contact_info_phone` |
| `dw_public.dim_person` | DW | `sk_person`, `id_person`, `id_user`, `uuid_person`, `person_name`, `person_email`, `person_phone`, `dt_birth`, `has_right_to_be_forgotten` |

**New enrich and DW tables must not store raw PII columns.** Instead, expose `sk_person` (DW) or `id_user` / `uuid_person` (enrich) as a foreign key. Downstream consumers who need contact details join `dw_public.dim_person` at query time.

### Prohibited in new enrich / DW tables

The following columns must never appear as stored columns in new enrich or DW SQL files when they can be sourced from `dim_person`:

- `cpf`, `rg` — document identifiers
- `nome`, `name`, `full_name`, `first_name`, `last_name`, `person_name` — personal name
- `email`, `person_email` — contact email
- `telefone`, `phone`, `person_phone` — contact phone
- `address`, `endereco`, `logradouro`, `cep`, `zip_code` — personal address
- `dt_birth`, `data_nascimento`, `dataNascimento` — date of birth
- `gender`, `sexo` — LGPD art. 11 sensitive category

**Exceptions (documented legal basis required):**

- Clean tables — mirror the source database for fidelity; PII is allowed but must not be propagated to enrich/DW.
- Fintech schemas (`cyber*`, `sorting_hat`, `recupera`) — CPF, RG, and mother/father names are required for credit analysis and debt collection under LGPD art. 7 §V. Each table must carry `personal_data_classification: personal` in metadata and `table_privileges` in the DAG declaration.
- Reverse DAGs — contact PII is required for external API transmission by design; must be sourced from `dim_person` (not `dim_user` or raw clean tables) and subject to the `has_right_to_be_forgotten` guard below.

### `has_right_to_be_forgotten` — Mandatory Guard for Reverse DAGs

Any reverse DAG query that exports personal data to an external system must include the following filter. Omitting it violates LGPD art. 18 §VI (right to erasure).

```sql
LEFT JOIN
    dw_public.dim_person AS dp
        ON source_table.id_user = dp.id_user
WHERE
    (dp.has_right_to_be_forgotten = false
    OR dp.has_right_to_be_forgotten IS NULL)
```

### Good Example — Enrich/DW using Person FK

```sql
-- Correct: store only the person FK; consumers join dim_person for contact details
SELECT
    rc.id_contract,
    rc.id_user,
    ps.sk_person,
    rc.status,
    rc.dt_signed
FROM
    datalake_ebdb_clean.contract AS rc
LEFT JOIN
    datalake_person.person_sks AS ps
        ON rc.id_user = ps.id_user
WHERE
    rc.dt_signed >= '{load_start_date}'
    AND rc.dt_signed < '{load_end_date}'
```

### Bad Example — Spreading PII into Enrich/DW

```sql
-- Forbidden: raw PII stored directly in an enrich or DW table
SELECT
    rc.id_contract,
    u.nome AS customer_name,
    u.email AS customer_email,
    u.cpf AS customer_cpf,
    rc.status
FROM
    datalake_ebdb_clean.contract AS rc
LEFT JOIN
    datalake_ebdb_clean.user AS u
        ON rc.id_user = u.id_user
```

---

## 14. Source Layer Policy by Target Layer

### Rule

SQL queries must prioritize upstream analytical layers according to the target layer:

- **Clean queries**: may read from `raw`.
- **Enrich queries**: should read from `clean` and/or other `enrich` tables.
- **DW queries**: should read from `enrich` tables (preferred immediate upstream layer).
- **Metric queries**: should read from `dw` tables (prefer non-datamart DW models).

### Hard constraints

- `raw` tables are intended to be consumed only by `clean` layer queries.
- `dw` queries should not use other `dw` tables, except explicit datamart DAGs.
- `metric` queries should not use datamart `dw` tables as primary sources.

### Good and Bad examples

**Good (DW from Enrich):**

```sql
SELECT
    ec.id_contract AS sk_contract,
    ec.id_user AS sk_user,
    ec.contract_status
FROM
    datalake_ebdb_contract.contract AS ec
```

**Bad (DW directly from Clean):**

```sql
SELECT
    c.id_contract AS sk_contract,
    c.id_user AS sk_user,
    c.status AS contract_status
FROM
    datalake_ebdb_clean.contract AS c
```

---

## SQL Style Checklist

- [ ] Columns are in English, lowercase, snake_case
- [ ] Columns arranged by priority
- [ ] All reserved words and functions are UPPERCASE
- [ ] Proper spacing around operators and commas
- [ ] New lines before AND/OR/ON, after SELECT/FROM/WHERE/JOIN
- [ ] Proper indentation (TAB or 2/4 spaces)
- [ ] Commas at end-of-line (right comma)
- [ ] Each column on its own row in SELECT
- [ ] No `SELECT *` — all columns enumerated explicitly (including inside CTEs, subqueries, and QUALIFY windows)
- [ ] CTEs used instead of subqueries
- [ ] Descriptive aliases with AS keyword
- [ ] Joins formatted with proper line breaks
- [ ] CASE-WHEN properly indented
- [ ] Comments formatted correctly (no blank lines)
- [ ] WHERE clause filters on partition/date columns for partitioned or large tables (enables partition pruning)
- [ ] Incremental loads use `{load_start_date}` / `{load_end_date}` params — no unbounded full scans
- [ ] Filters applied inside CTEs before joins rather than in the outermost query
- [ ] Enrich/DW tables use `sk_person` / `id_person` / `uuid_person` as FK — no raw PII columns (`name`/`email`/`phone`/`cpf`/`rg`/`address`) stored in enrich or DW tables
- [ ] Reverse DAG queries include a `has_right_to_be_forgotten = false` guard via `dim_person` before exporting any personal data to external systems
- [ ] `raw` tables are consumed only by clean-layer queries
- [ ] Enrich queries use clean/enrich sources (not raw)
- [ ] DW queries prioritize enrich sources and avoid DW-to-DW reads (except explicit datamarts)
- [ ] Metric queries prioritize DW sources and avoid datamart DW tables as primary inputs
