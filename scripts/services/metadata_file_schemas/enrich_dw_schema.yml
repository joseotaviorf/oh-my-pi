---
database_name: str(min=1, max=280)
table_name: str(min=1, max=280)
owner: regex('(?:[\.-]?\w+)*@\w+(?:[\.-]?\w+)*')
domain: regex('3P Partners|Agents|Cross|Data Ops & Governance|Data Life Cycle|Fintech|For Rent|For Sale|Growth|International|MLOps|People|Primitives|Rede|Support and Services|Tech Platform|Conversational XP')
description: str(min=10)
columns: map(include("column"), key=str(min=1, max=280))
---
column:
  description: str(min=10)
  lineage: list(str(min=1, max=280), required=False)
  categories: map(str(), key=str(), required=False)