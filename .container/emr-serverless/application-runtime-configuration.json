{
  "_comment": [
    "Application-level Spark defaults for the SageMaker Unified Studio notebook",
    "EMR Serverless application. This is the AUTHORITATIVE source for notebook",
    "Spark configuration — values baked into the image (spark-defaults-quintoandar.conf)",
    "are defence in depth only, because EMR Serverless regenerates spark-defaults.conf",
    "when it launches a worker.",
    "",
    "Precedence, lowest to highest:",
    "  1. image spark-defaults.conf   (best effort)",
    "  2. application runtimeConfiguration  <- this file",
    "  3. job-level / %%configure in a notebook  (user override, always wins)",
    "",
    "Apply with:",
    "  aws emr-serverless update-application \\",
    "    --application-id <app-id> \\",
    "    --runtime-configuration file://application-runtime-configuration.json",
    "",
    "The application must be STOPPED to update. Users need no notebook config",
    "for anything set here."
  ],
  "applicationConfiguration": [
    {
      "classification": "spark-defaults",
      "properties": {
        "spark.sql.session.timeZone": "UTC"
      }
    }
  ]
}
