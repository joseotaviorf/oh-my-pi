# Private wheels baked into the EMR Serverless notebook image.
# Paths are relative to ARTIFACTS_BUCKET (default: s3://artifacts.s3.data.quintoandar.com.br).
#
# Adding a client here makes it available to every notebook with zero config.
# Leaving one out does NOT block anyone — a notebook can always pull it via
# spark.submit.pyFiles, which takes precedence over anything installed here.
# See docs/platform/emr_serverless_notebook_image.md.
#
# Selection rule: bake in clients used by more than one DAG. Single-use clients
# stay out to keep the image small; their owners use %%configure.

# --- bietlejuice core stack (mirrors default_libraries in prod_conf.yml) ---
bi-etl-ejuice/bietlejuice_core-latest-py3-none-any.whl
bi-etl-ejuice/bietlejuice_runtime-latest-py3-none-any.whl
python-logger/quintoandar_logger-0.8.0-py3-none-any.whl
inmetro/inmetro-4.10.1-py3-none-any.whl

# --- shared API clients (>1 consuming DAG) ---
# gsheets: 28 DAGs. 0.3.0 is still referenced by one legacy DAG; that DAG should
# override via pyFiles rather than hold the image back.
gsheets-api-client-python/quintoandar_gsheets_api_client-0.7.0-py2.py3-none-any.whl
survicate-api-client-python/quintoandar_survicate_api_client-0.1.0-py2.py3-none-any.whl
velo-neurotech-api-client-python/quintoandar_velo_neurotech_api_client-0.3.2-py2.py3-none-any.whl
omie-api-client-python/quintoandar_omie_api_client-0.4.0-py2.py3-none-any.whl
braze-api-client-python/quintoandar_braze_api_client-0.1.0-py2.py3-none-any.whl

# --- single-use clients: intentionally NOT baked in ---
# airtable, criteo, facebook, hr_system, hubspot, jira, reclameaqui,
# sap_4hana, tracksale, twilio_flex_insights
# Pull them from a notebook with:
#   %%configure -n <compute> -f
#   {"conf": {"spark.submit.pyFiles": "s3://.../quintoandar_<name>_api_client-<ver>-py2.py3-none-any.whl"}}
