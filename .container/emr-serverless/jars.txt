# JARs copied onto the Spark classpath. Relative to ARTIFACTS_BUCKET.
# Mirrors the JAR block in packages/bietlejuice-compiler/scripts/emr_init_script.sh,
# plus the JDBC drivers that DAGs declare via cluster.custom_libraries.
#
# Missing keys are skipped with a warning rather than failing the build — same
# behaviour as emr_init_script.sh, so a rotated JAR name does not block the image.

# --- JDBC drivers (from dags/**/*_cluster.yml custom_libraries) ---
jars/ojdbc8.jar
jars/mssql-jdbc-12.8.1.jre8.jar
jars/mysql-connector-java-8.0.30.jar
jars/postgresql-42.7.3.jar

# --- data quality / observability (from emr_init_script.sh) ---
jars/deequ-2.0.8-spark-3.5.jar
jars/spark-measure_2.12-0.21.jar
jars/spark-plugins_2.12-0.2.jar
jars/spark-cluster-metrics_2.12-0.1-SNAPSHOT.jar
jars/kafka-clients-3.5.0.jar
jars/openlineage-spark_2.12-1.46.0.jar
