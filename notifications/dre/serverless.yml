service: dre-costs

provider:
  name: aws
  runtime: python2.7
  stage: dev
  region: us-east-1

functions:
  lastUpdateDate:
    handler: handler.dre_costs_last_update_date
    events:
      - schedule: cron(0 0 5,10 * ? *)