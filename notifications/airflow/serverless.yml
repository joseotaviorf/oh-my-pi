service: airflow

provider:
  name: aws
  runtime: python2.7
  stage: prod
  region: us-east-1

functions:
  failedJobs:
    handler: handler.failed_jobs
    name: airflow-failed-jobs
    description: Checks failed airflow jobs
    events:
      - schedule: cron(0 3 1/1 * ? *)
