# Path filters use Woodpecker `when.path` with `event: [push, pull_request]`.
# See validations.yml header for the full rationale — pull_request is required
# so that when.path evaluates the full PR diff, not just the last pushed commit.
clone:
  - name: clone
    image: woodpeckerci/plugin-git
    settings:
      partial: false # We need to clone the entire history to find the changed files

steps:
  setup:
    image: 796143582747.dkr.ecr.us-east-1.amazonaws.com/quintoandar/bi-etl-ejuice:prod-ci-jdk-latest
    pull: true
    commands:
      - uv sync --directory packages/bietlejuice-core
      - uv sync --directory packages/bietlejuice-airflow
      # Runtime is a standalone uv project; sync it once for test steps.
      - uv sync --directory packages/bietlejuice-runtime
      # DBR-pinned tests run from the dbr-16-4 sub-project's venv. It mirrors
      # DBR 16.4 LTS bundled libs (numpy, pandas, pyarrow, psycopg2, pydantic, ...)
      # so CI imports resolve to the same versions seen on the cluster. Other
      # DBRs (12.2 / 13.3) are not yet matrix-tested.
      - uv sync --directory packages/bietlejuice-runtime/envs/dbr-16-4
      - uv sync --directory packages/bietlejuice-compiler
    when:
      event: [push, pull_request]

  # PR pushes: scope tests to changed modules via pytest-testmon + path detection.
  # master/forno pushes run the full suite via unit-tests-python instead.
  changed-unit-tests-python:
    image: 796143582747.dkr.ecr.us-east-1.amazonaws.com/quintoandar/bi-etl-ejuice:prod-ci-jdk-latest
    pull: true
    commands:
      - export AWS_DEFAULT_REGION=us-east-1
      - make unit-tests-changed
    when:
      event: [push, pull_request]
      branch:
        exclude: [master, forno]
      path:
        include:
          - '**/*.py'
          - 'pyproject.toml'
          - 'packages/*/pyproject.toml'
          - 'uv.lock'
        on_empty: false
    depends_on: [setup]

  # master/forno pushes: run the full suite and rebuild .testmondata as the CI baseline.
  unit-tests-python:
    image: 796143582747.dkr.ecr.us-east-1.amazonaws.com/quintoandar/bi-etl-ejuice:prod-ci-jdk-latest
    pull: true
    commands:
      - export AWS_DEFAULT_REGION=us-east-1
      - make unit-tests
    when:
      event: [push, pull_request]
      branch: [master, forno]
      path:
        include:
          - '**/*.py'
          - 'pyproject.toml'
          - 'packages/*/pyproject.toml'
          - 'uv.lock'
        on_empty: false
    depends_on: [setup]

  integration-tests-python:
    image: 796143582747.dkr.ecr.us-east-1.amazonaws.com/quintoandar/bi-etl-ejuice:prod-ci-jdk-latest
    pull: true
    commands:
      - export AWS_DEFAULT_REGION=us-east-1
      - make integration-tests
    when:
      event: [push, pull_request]
      path:
        include:
          - '**/*.py'
          - 'pyproject.toml'
          - 'packages/*/pyproject.toml'
          - 'uv.lock'
        on_empty: false
    depends_on: [setup]

  core-model-tests-and-coverage:
    image: 796143582747.dkr.ecr.us-east-1.amazonaws.com/quintoandar/bi-etl-ejuice:prod-ci-jdk-latest
    pull: true
    commands:
      - export AWS_DEFAULT_REGION=us-east-1
      - make core-model-coverage
    when:
      event: [push, pull_request]
      path:
        include:
          - 'dags/core/**/*.py'
          - 'packages/bietlejuice-runtime/test/**/*.py'
          - 'packages/bietlejuice-runtime/src/bietlejuice/base/core_models/**/*.py'
          - 'packages/bietlejuice-airflow/src/bietlejuice/base/core_models/**/*.py'
          - 'packages/bietlejuice-core/src/bietlejuice/base/core_models/**/*.py'
        on_empty: false
    depends_on: [setup]
