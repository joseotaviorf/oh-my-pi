drop table if exists lead;
create table if not exists lead (
  id bigint not null,
  anuncio_criado_em date,
  area_total integer,
  bairro varchar(200),
  captado_em date,
  cep varchar(9),
  cidade varchar(200),
  complemento varchar(200),
  endereco varchar(200),
  endereco_captado text,
  nome_anunciante varchar(255),
  numero varchar(200),
  numero_banheiros integer,
  numero_quartos integer,
  numero_suites integer,
  url_anuncio varchar(255),
  valor integer,
  telefone_anunciante varchar(100),
  valor_por_metro_quad numeric(18,4),
  valor_por_quartos numeric(18,4),
  tipo varchar(255),
  email varchar(250),
  email_captador varchar(250),
  telefone_captador varchar(250),
  hot integer,
  proximo_followup timestamp,
  gerado_a_partir_de_deduplicacao integer,
  dentro_area_atuacao integer default 0,
  sistema_enviou_email_via_classificado integer,
  lat numeric(10,7),
  lng numeric(10,7),
  condominio integer,
  iptu integer,
  reason varchar(255),
  reason_detail varchar(255),
  status varchar(255),
  envio_email_apresentacao_pos timestamp,
  envio_email_apresentacao_pre timestamp,
  processado smallint,
  origem varchar(255),
  external_id varchar(255),
  mencionar integer,
  referencia varchar(255),
  automatically_discarded integer,
  estado_nome varchar(255),
  estado_abrev varchar(255),
  lead_imobiliaria varchar(255),
  proprietario_nome varchar(255),
  proprietario_email varchar(255),
  dados_corretor_tipo_afiliado varchar(200),
  dados_corretor_nome varchar(250),
  dados_corretor_email varchar(250),
  dados_gerente_contas_nome varchar(250),
  dados_gerente_contas_email varchar(250),
  affiliate_type varchar(255),
  dados_afiliado_inicio_atuacao timestamp,
  dados_afiliado_cidade_atuacao varchar(100),
  region_id integer,
  atualizado_em timestamp,
  criado_em timestamp,
  url_source varchar(255),
  utm_medium varchar(255),
  utm_campaign varchar(255),
  utm_source varchar(255),
  usuario_que_indicou_id bigint,
  codigo_imobiliaria varchar(255),
  flg_city_served boolean,
  flg_latlng_served boolean,
  flg_location_served boolean,
  flg_b2b boolean
);

create unique index lead_id_idx on lead (id);
CREATE INDEX lead_usuario_que_indicou_id_idx ON public."lead" (usuario_que_indicou_id);
CREATE INDEX lead_telefone_anunciante_idx ON public."lead" (telefone_anunciante);
CREATE INDEX lead_cidade_idx ON public."lead" (cidade);

