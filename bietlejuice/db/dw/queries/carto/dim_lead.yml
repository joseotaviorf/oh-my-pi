---
carto_table_name: dim_lead
sql_filename: dim_lead
final_sql: >-
  UPDATE dim_lead
  SET the_geom = ST_SetSRID(ST_MakePoint(lng::float, lat::float), 4326)
  WHERE the_geom IS NULL
  AND COALESCE(lng, '') != '' AND COALESCE(lat, '') != ''
