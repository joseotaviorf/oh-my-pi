---
database_name: dw_datamarts
table_name: offboarding_repairs
columns:
  sk_contract:
    lineage:
      - datalake_jira_clean.issues.fields
  sk_city:
    lineage:
      - dw_public.dim_region.city_id
  key:
    lineage:
      - datalake_jira_clean.issues.key
  priority:
    lineage:
      - datalake_jira_clean.issues.fields
  status:
    lineage:
      - datalake_jira_clean.issues.fields
  resolution:
    lineage:
      - datalake_jira_clean.issues.fields
  company:
    lineage:
      - datalake_jira_clean.issues.fields
  total_repair_value:
    lineage:
      - datalake_jira_clean.issues.fields
  city_group:
    lineage:
      - dw_public.dim_region.city_group
  city_name:
    lineage:
      - dw_public.dim_region.city_name
  name:
    lineage:
      - dw_public.dim_region.name
  regional:
    lineage:
      - dw_public.dim_region.regional
  regional_inspection:
    lineage:
      - dw_public.dim_region.regional_inspection
  region_code:
    lineage:
      - dw_public.dim_region.region_code
  region_code_inspector:
    lineage:
      - dw_public.dim_region.region_code_inspector
  tier:
    lineage:
      - dw_public.dim_region.tier
  dt_partner_approved:
    lineage:
      - datalake_jira_clean.issues.fields
  dt_repair_completed:
    lineage:
      - datalake_jira_clean.issues.fields
  dt_repair_booked:
    lineage:
      - datalake_jira_clean.issues.fields
  dt_budget_done:
    lineage:
      - datalake_jira_clean.issues.fields
  ts_created:
    lineage:
      - datalake_jira_clean.issues.fields
  ts_resolution:
    lineage:
      - datalake_jira_clean.issues.fields
  ts_updated:
    lineage:
      - datalake_jira_clean.issues.fields