---
database_name: dw_datamarts
table_name: em_casa_house_listing_flows
columns:
  id_house:
    lineage:
      - datalake_crawlers_listings_clean.em_casa.id_house
  id_house_platform:
    lineage:
      - datalake_crawlers_listings_clean.em_casa.id_house_platform
  id_neighborhood:
    lineage:
      - datalake_ebdb_clean.map_region.id_neighborhood
  neighborhood:
    lineage:
      - datalake_crawlers_listings_clean.em_casa.neighborhood
      - datalake_ebdb_clean.polygon_region.neighborhood
  ts_started_date:
    lineage:
      - dw_public.dim_date.week_start
  ts_ended_date:
    lineage:
      - dw_public.dim_date.week_start
  ts_load:
    lineage:
      - dw_public.dim_date.week_start