WITH leads_last_version AS (
    SELECT 
        ROW_NUMBER() OVER(PARTITION BY id_lead ORDER BY ts_updated DESC) AS ordered_version,
        id_lead,
        id_admin,
        id_airtable_record,
        accreditations,
        activation,
        actuation_model,
        actuation_preference,
        actuation_state,
        adjusted_cpf,
        agent_creci,
        approved_status,
        checked_by,
        chosen_region,
        creci_situation,
        creci_status,
        grade,
        how_met_5a,
        lead_email,
        lead_name,
        lead_phone_number,
        permanent_deaccreditation,
        real_estate,
        region_first_option,
        region_second_option,
        region_first_option_complete,
        region_second_option_complete,
        rental_status,
        rental_activated,
        training_giving_up,
        is_duplicated,
        dt_answer,
        dt_auto_data_creci,
        dt_birth,
        dt_contract_sent,
        dt_fup_call,
        dt_fup_email_sent,
        dt_fup_sms_sent,
        dt_fup_whatsapp_sent,
        dt_id_created,
        dt_reforce_sent,
        dt_signed,
        dt_subscripted,
        dt_training_giving_up,
        dt_training_sent,
        ts_updated,
        year,
        month,
        day
    FROM
        datalake_airtable_clean.leads
)
SELECT
    id_lead,
    id_admin,
    id_airtable_record,
    accreditations,
    activation,
    actuation_model,
    actuation_preference,
    actuation_state,
    adjusted_cpf,
    agent_creci,
    approved_status,
    checked_by,
    chosen_region,
    creci_situation,
    creci_status,
    grade,
    how_met_5a,
    lead_email,
    lead_name,
    lead_phone_number,
    permanent_deaccreditation,
    real_estate,
    region_first_option,
    region_second_option,
    region_first_option_complete,
    region_second_option_complete,
    rental_status,
    rental_activated,
    training_giving_up,
    is_duplicated,
    dt_answer,
    dt_auto_data_creci,
    dt_birth,
    dt_contract_sent,
    dt_fup_call,
    dt_fup_email_sent,
    dt_fup_sms_sent,
    dt_fup_whatsapp_sent,
    dt_id_created,
    dt_reforce_sent,
    dt_signed,
    dt_subscripted,
    dt_training_giving_up,
    dt_training_sent,
    ts_updated,
    year,
    month,
    day
FROM
    leads_last_version
WHERE
    ordered_version = 1