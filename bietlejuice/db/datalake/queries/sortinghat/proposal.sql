SELECT
    id,
    id_house as house_id,
    date(dt_analyzed) as analysis_date,
    score_5A,
    score_5A_best_subset,
    score_cardif,
    score_cardif_best_subset,
    status,
    comment,
    analyst_name,
    supervisor_name,
    rent_value,
    condo_value,
    iptu_value,
    date(ts_created) as created_at,
    date(ts_updated) as updated_at,
    home_area,
    home_bathrooms,
    home_city,
    home_garages,
    home_region,
    home_suites,
    home_type,
    home_zipcode,
    id_drive,
    rejection_motive,
    home_insurance_value,
    risk_level,
    risk_level_best_subset,
    date(dt_processed) as process_date,
    home_exclusive_5A,
    dt_home_last_published,
    bypass_pre_analysis,
    pre_analysis_result,
    pre_analysis_reason
FROM datalake_sorting_hat_clean_prod.proposal proposal