redshift_template:
  dw: &redshift_anchor
    schema: datamarts
    runs_on: redshift
athena_template:
  dw: &athena_anchor
    schema: datamarts
    runs_on: athena
pipeline:
  ##<KEEP THIS COMMENTED AS A TEMPLATE>
  # <task-name-like-attribution-conversion-paths>:
  #   dw:
  #     sql_file: <query-file E.g: dw/queries/datamarts/offer_attribution_conversion_paths_demand.sql>
  #     table: <targe-table-name>
  #     schema: <target-schema-name> # TODO this will always be `datamart` (for both datalake and dw). This var is not used anymore
  #     runs_on: <backend-to-run-the-query> E.g: redshift or athena
  # example_redshift_weekly_demand_metrics:
  #   dw:
  #     <<: *redshift_anchor
  #     sql_file: dw/queries/datamarts/for_rent/weekly_demand_metrics.sql
  #     table: weekly_demand_metrics
  # example_athena_house_weekly_entrance_info:
  #   dw:
  #     <<: *athena_anchor
  #     sql_file: dw/queries/datamarts/for_rent/house_weekly_entrance_info.sql
  #     table: house_weekly_entrance_info
  attribution_conversion_paths_demand:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/growth/attribution_conversion_paths_demand.sql
      table: attribution_conversion_paths_demand
  buyer_activation_events_combo:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/growth/buyer_activation_events_combo.sql
      table: buyer_activation_events_combo
  daily_target_volumes_supply:
    dw:
      <<: *redshift_anchor
      sql_file: dw/queries/datamarts/growth/daily_target_volumes_supply.sql
      table: daily_target_volumes_supply
  portal_casa_mineira_advertiser_metrics:
    dw:
      <<: *redshift_anchor
      sql_file: dw/queries/datamarts/growth/portal_casa_mineira_advertiser_metrics.sql
      table: portal_casa_mineira_advertiser_metrics
  unique_owners:
    dw:
      <<: *redshift_anchor
      sql_file: dw/queries/datamarts/growth/unique_owners.sql
      table: unique_owners
  users_online_metrics:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/growth/users_online_metrics.sql
      table: users_online_metrics
  sale_performance_marketing_metrics_supply_cohort:
    dw:
      <<: *redshift_anchor
      sql_file: dw/queries/datamarts/growth/sale_performance_marketing_metrics_supply_cohort.sql
      table: sale_performance_marketing_metrics_supply_cohort
  tof_demand_online_events:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/growth/tof_demand_online_events.sql
      table: tof_demand_online_events