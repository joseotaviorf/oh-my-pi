redshift_template:
  dw: &redshift_anchor
    schema: datamarts
    runs_on: redshift
athena_template:
  dw: &athena_anchor
    schema: datamarts
    runs_on: athena
pipeline:
  ##<KEEP THIS COMMENTED AS A TEMPLATE>
  # <task-name-like-attribution-conversion-paths>:
  #   dw:
  #     sql_file: <query-file E.g: dw/queries/datamarts/offer_attribution_conversion_paths_demand.sql>
  #     table: <targe-table-name>
  #     schema: <target-schema-name> # TODO this will always be `datamart` (for both datalake and dw). This var is not used anymore
  #     runs_on: <backend-to-run-the-query> E.g: redshift or athena
  # example_redshift_weekly_demand_metrics:
  #   dw:
  #     <<: *redshift_anchor
  #     sql_file: dw/queries/datamarts/support_and_service/weekly_demand_metrics.sql
  #     table: weekly_demand_metrics
  # example_athena_house_weekly_entrance_info:
  #   dw:
  #     <<: *athena_anchor
  #     sql_file: dw/queries/datamarts/for_rent/house_weekly_entrance_info.sql
  #     table: house_weekly_entrance_info
  ongoing_refund_requests:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/support_and_service/ongoing_refund_requests.sql
      table: ongoing_refund_requests
  house_media:
    dw:
      <<: *athena_anchor
      sql_file: dw/queries/datamarts/support_and_service/house_media.sql
      table: house_media
  repressed_demand_booking_fit_in:
    dw:
      <<: *redshift_anchor
      sql_file: dw/queries/datamarts/support_and_service/repressed_demand_booking_fit_in.sql
      table: repressed_demand_booking_fit_in
