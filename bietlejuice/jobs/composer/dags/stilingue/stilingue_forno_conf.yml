---
# entry example:
# endpoint_schema_exceptions:
#   <endpointEnum_name>: <schema>
endpoint_schema_exceptions:
  LISTENING_LINECHART_JSON_KEYS: ["datetime_reference", "value"]

# entry example:
# tables:
#   <raw_table_name>:
#     api_endpoint: <api_endpoint>
#     api_params: <api_params>
#     clean_table_name: <clean_table_name>
#     partition_cols: <partition_cols> (Optional)
tables:
  report_repliers:
    api_endpoint: SAC_REPORT_REPLIES_JSON_KEYS
    api_params: {
      channels: facebook,
      fanpages: 441491169250837,
      date_range: '{year}{month}{day}0000:{year}{month}{day}2359',
    }
    clean_table_name: report_repliers
    partitions_cols: ['year', 'month', 'day']

  calls_report:
    api_endpoint: CALLS_REPORT_JSON_KEYS
    api_params: {
      format: json,
      from_processed: True,
      date_range: '{year}{month}{day}0000:{year}{month}{day}2359'
    }
    clean_table_name: calls_report
    is_array_data_schema: True
    partitions_cols: ['year', 'month', 'day']
