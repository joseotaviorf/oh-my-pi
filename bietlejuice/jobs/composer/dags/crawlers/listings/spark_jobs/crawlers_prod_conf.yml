---
root_path: "s3://listing-crawler.s3.data.quintoandar.com.br/full/"
job_extra_args:
  consumer:
    format: json
  custom_records_per_file:
    loft: 300000
partition_cols: ["city", "year", "month", "day"]
