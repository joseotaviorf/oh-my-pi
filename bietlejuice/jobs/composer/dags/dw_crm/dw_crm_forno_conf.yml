---
full_tables:
  - dim_closing_task
  - dim_credit_task
  - dim_inspection_task
  - dim_lead
  - dim_linhadireta_chat
  - dim_payment_task
  - dim_offboarding
  - dim_onboarding
  - dim_photo_job
  - dim_repair
  - dim_ungrouped
  - dim_visit
  - fact_lead_tasks
incremental_load_parameters:
  dw_query_filters:
    year: "{year}"
    month: "{month}"
    day: "{day}"
  partitions: [year, month, day]
inner_dependencies:
  fact_lead_tasks:
    - fact_lead_task_actions
