# Temporary file for controlling the turning on of the Hive Metastore sync task
#   in the DAGs. For activating the task sync-hive-metastore-table for a DAG
#   (that uses DatalakeSubdag, DWSubdag or IncrementalDWSubDAG) remove the DAG
#   from the list bellow.

