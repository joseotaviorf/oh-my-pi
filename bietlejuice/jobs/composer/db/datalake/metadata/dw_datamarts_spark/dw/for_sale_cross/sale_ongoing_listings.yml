---
database_name: dw_datamarts
table_name: sale_ongoing_listings
columns:
  weekday_name:
    lineage:
      - dw_public.dim_date.weekday_name
  week_start:
    lineage:
      - dw_public.dim_date.week_start
  sk_region:
    lineage:
      - dw_sale.fact_listing_status.sk_region
  region:
    lineage:
      - dw_public.dim_region.name
  city_name:
    lineage:
      - dw_public.dim_region.city_name
  city_group:
    lineage:
      - dw_public.dim_region.city_group
  is_3p_supply:
    lineage:
      - dw_public.dim_house_listing.is_3p_supply
  supply_3p_partner:
    lineage:
      - dw_public.dim_house_listing.partner_3p_supply
