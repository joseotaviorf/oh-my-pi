select
    business_context,
    dimension_name,
    dimension_value,
    metric_name,
    baseline,
    treatment,
    date,
    hypothesis_test,
    params,
    baseline_observations,
    baseline_conversions,
    treatment_observations,
    treatment_conversions,
    binary_or_continuous,
    baseline_conversion_rate,
    treatment_conversion_rate,
    conversion_rate_diff_abs,
    conversion_rate_diff_rel,
    ci_lower_bound_rel,
    ci_upper_bound_rel,
    mde_rel,
    significance,
    has_significance,
    p_treatment_win,
    decision,
    baseline_observations_cumulative,
    baseline_conversions_cumulative,
    treatment_observations_cumulative,
    treatment_conversions_cumulative,
    baseline_conversion_rate_cumulative,
    treatment_conversion_rate_cumulative,
    conversion_rate_diff_abs_cumulative,
    conversion_rate_diff_rel_cumulative,
    ci_lower_bound_rel_marginal,
    ci_upper_bound_rel_marginal,
    significance_marginal,
    exp_name
from
  datalake_search_metrics_service_raw.search_experiments
