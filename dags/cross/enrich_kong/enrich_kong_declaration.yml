dag:
    name: enrich_kong
    schedule_start_date: 2023, 4, 3
    owner: Data SS
    documentation: 
        dag_purpose: |
            Extracts the access logs message in to columns
workflow:
    type: query
    layer: enrich
    default_extraction_type: incremental
    default_partitions: ["year", "month", "day", "hour"]
cluster:
    type: databricks_10_4_max_io-memory_cluster
