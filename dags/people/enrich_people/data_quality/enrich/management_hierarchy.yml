---
table_name: datalake_people.management_hierarchy
alert_channel: PEOPLE_ALERTS

table_level_validations:
  has_size:
    greater_than: 0
    severity_level: Error
  has_size_variation:
    variation_type: percentage
    greater_than_or_equal_to: 0
    less_than_or_equal_to: 25
    severity_level: Warning

column_level_validations:
  sk_hierarchy_version:
    is_complete:
      severity_level: Error
    is_unique:
      severity_level: Warning

  assignment_number:
    is_complete:
      severity_level: Error

  assignment_number_l0:
    is_complete:
      severity_level: Error

  version:
    is_complete:
      severity_level: Warning
  minimum_version_at_least_one:
    custom:
      # Ensures the minimum version number is at least 1. Data should have been processed at least once.
      constraint: |-
        (SELECT COALESCE(MIN(version), 0)
         FROM datalake_people.management_hierarchy) >= 1
      severity_level: Warning

  one_current_version_per_assignment:
    custom:
      # Ensures each assignment has at most one current version. No duplicate is_current = TRUE per assignment_number.
      constraint: |-
        (SELECT COUNT(*)
         FROM (
           SELECT assignment_number, COUNT(*) AS current_count
           FROM datalake_people.management_hierarchy
           WHERE is_current = TRUE
           GROUP BY assignment_number
           HAVING current_count > 1
         )
        ) = 0
      severity_level: Error

  dt_valid_from:
    is_complete:
      severity_level: Error

  dt_valid_to:
    is_complete:
      severity_level: Error

  ts_load:
    is_complete:
      severity_level: Warning
