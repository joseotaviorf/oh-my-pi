---
table_name: datalake_pin_compensation_clean.element_entry_value
alert_channel: PEOPLE_ALERTS
table_level_validations:
  has_size:
    greater_than: 1
    severity_level: Error
column_level_validations:
  id_element_entry_value:
    is_complete:
      severity_level: Warning
    custom:
      constraint: "(SELECT COUNT(*) FROM (SELECT 1 FROM datalake_pin_compensation_clean.element_entry_value GROUP BY id_element_entry_value, object_version_number HAVING COUNT(*) > 1)) = 0"
      severity_level: Warning
  id_element_entry:
    is_complete:
      severity_level: Warning
  id_input_value:
    is_complete:
      severity_level: Warning
