---
table_name: datalake_pin_core_clean.flexfield_context_translation
alert_channel: PEOPLE_ALERTS
table_level_validations:
  has_size:
    greater_than: 1
    severity_level: Error

column_level_validations:
  id_application:
    is_complete:
      severity_level: Warning
    custom:
      constraint: "(SELECT COUNT(*) FROM (SELECT 1 FROM datalake_pin_core_clean.flexfield_context_translation GROUP BY id_application, language, ts_updated HAVING COUNT(*) > 1)) = 0"
      severity_level: Warning