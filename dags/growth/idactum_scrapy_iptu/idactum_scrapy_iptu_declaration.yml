dag:
  name: idactum_scrapy_iptu
  owner: Data Growth
  schedule_start_date: 2026, 05, 07
  documentation:
    dag_purpose: |
      Ingest Idactum Scrapy IPTU crawler outputs (Osasco extrato IPTU, Brasilia debitos and
      pauta) from crawler-scrapy-{forno|prod} into the shared crawled_idactum_houses raw/clean
      databases. Osasco extrato remains passthrough until PDF/OCR extraction is implemented.
workflow:
  layer: raw
  type: custom_ingestion
  custom_schema: crawled_idactum_houses
  spark_job_prefix: crawled_idactum_houses
  load_spark_job: load_idactum_scrapy_raw
  default_extraction_type: full
  default_partitions: [year, month, day]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_start_date') }}"
  spark_job_arguments:
    - "{environment}"
    - "{bucket}"
    - "{table_name}"
    - "{load_start_date}"
    - "crawled_idactum_houses"
    - "{table_name}"
  tables_customization:
    sp_osasco_extrato_iptu:
      partitions: [year, month, day]
    bsb_main_debitos:
      partitions: [year, month, day]
    bsb_main_pauta:
      partitions: [year, month, day]
