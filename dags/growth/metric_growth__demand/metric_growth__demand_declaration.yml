dag:
  name: metric_growth__demand
  schedule_start_date: 2024,04,15
  owner: Data Growth
  criticality: High
  sla_deadline_localtime: "08:00"
workflow:
  tables_customization:
    growth_demand_performance_daily:
      criticality: Critical
    growth_demand_performance_monthly:
      criticality: Critical
  type: query
  layer: metric
  custom_schema: growth
  default_extraction_type: incremental
  default_partitions: [year, month, day]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, macros.ds_add(data_interval_start | ds, -14), 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_end_date') }}"
