dag:
  name: dw_affiliate_costs
  owner: Data Growth
  documentation:
    dag_purpose: |
      This DAG creates the dim and fact that the Marketing Analysts will use to analyze the commission costs that
      occur in our Indica AI Program.
workflow:
  type: query_delta
  layer: dw
  custom_schema: quintoandar
cluster:
  type: databricks_12_2_med_general_photon_cluster
  access_control_list:
    group_name: analytics-engineers
    permission_level: CAN_MANAGE
  databricks_conn_id: databricks_new
  custom_configurations:
    single_user_name: '{{ var.value.databricks_single_user_name }}'
    data_security_mode: SINGLE_USER
    spark_conf:
      spark.databricks.sql.initial.catalog.namespace: quintoandar_{{ var.value.environment }}
