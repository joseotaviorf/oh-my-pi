---
dag:
  name: realestate_demand
  owner: Data Growth
  schedule_interval: 0 8,12,21 * * *
  schedule_start_date: 2026, 9, 1
  documentation:
    dag_purpose: |-
      CDC pipeline for RealEstate (Navent) MySQL — demand context. Part of the Navent → QuintoAndar migration POC (RD-3335, parent RD-3332). CDC ingestion of RealEstate lead-generation events: buyer/renter contact actions captured when users reach out about a listing.

      DATA LINEAGE: MySQL (OLTP) → datalake_realestate_raw → datalake_realestate_clean
      SCHEDULE: 3× daily at minute 0 UTC (08:00, 12:00, 21:00).
workflow:
  type: cdc
  layer: raw
  database_type: mysql
  source_database: realestate
  custom_schema: realestate
  source_schema: realestate
  dbutils_secret_key: REALESTATE_DB
  tables_customization:
    contactosacciones:
      clean_table_name: contact_actions
      clean_primary_keys:
      - id_contact_action
      clean_z_order_by:
      - id_contact
  extra_query_template_params:
    load_start_date: '{{ get_date_param(dag_run, data_interval_start | ds, ''load_start_date'') }}'
    load_end_date: '{{ get_date_param(dag_run, data_interval_start | ds, ''load_end_date'') }}'
