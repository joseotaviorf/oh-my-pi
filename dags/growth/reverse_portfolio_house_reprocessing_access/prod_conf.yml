---
dag_documentation:
  dag_purpose: |
    This DAG retrieves data stored in the `reverse_house_portfolio_reprocessing` schema,
    which was previously processed by the DAG `reverse_house_portfolio_reprocessing`, and transmits
    it through Kafka. This dataset serves as the basis for the segmentation in our Lead Management Service. 

database_name: reverse_house_portfolio_reprocessing
topic_sufix: "event.Reprocessing"
table_name: "ebdb_houses_formatted"
kafka:
  bootstrap_servers: "lkc-256gpm-jv0e34.us-east-1.aws.glb.confluent.cloud:9092"