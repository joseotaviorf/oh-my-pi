---
database_name: datalake_demand_contact_submission_clean
table_name: contact_submission_types
domain: Growth
owner: gabriel.salla@quintoandar.com
description: Clean layer for demand contact submission type lookup from the contact submission service. One row per contact type with version and audit timestamps.
columns:
  id_contact_submission_type:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.id
    description: Primary key of the contact submission type.
  type:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.type
    description: Human-readable label of the contact submission type.
  version:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.version
    description: Optimistic lock version from the source service.
  ts_created_at:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.created_at
    description: Timestamp when the type record was created.
  ts_updated_at:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.updated_at
    description: Timestamp when the type record was last updated.
  year:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.created_at
    description: Hive partition year derived from ts_created_at for incremental loads.
  month:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.created_at
    description: Hive partition month derived from ts_created_at for incremental loads.
  day:
    lineage:
      - datalake_demand_contact_submission_raw.contact_submission_types.created_at
    description: Hive partition day derived from ts_created_at for incremental loads.
