dag:
  name: reverse_demand_classifieds_ranking
  schedule_start_date: 2024,10,09
  owner: Data Growth
  documentation:
    dag_purpose: |
      This DAG is responsible for loading the reverse demand score data into the reverse demand score table.
workflow:
  type: load
  layer: reverse
  custom_schema: demand_score
  default_extraction_type: full
