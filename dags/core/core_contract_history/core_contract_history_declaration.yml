dag:
  name: core_contract_history
  schedule_start_date: 2024, 1, 1
  owner: Data ForRent
  documentation:
    dag_purpose: |
      Creates the Contract Historical Core Model (narrow event-log from CDC).
      Each row represents a single field-level change detected from the
      transactional layer, using a fixed 13-column schema.
workflow:
  type: core_model
  layer: core
  custom_schema: core_contract
  default_extraction_type: incremental
  load_spark_job: load_core_contract_history
  execution_timeout_hours: 1
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, macros.ds_add(data_interval_start | ds, 1), 'load_end_date') }}"
  tables_customization:
    contract_history:
      spark_job_arguments:
        - "{environment}"
        - "{bucket}"
        - "{dag_name}"
        - "{schema}"
        - "{table_name}"
        - "{partitions}"
        - "{load_start_date}"
        - "{load_end_date}"
      partitions: [year, month, day]
      z_order_by: [id_contract, ts_transaction]
      table_properties:
        "delta.enableChangeDataFeed": "true"
cluster:
  type: databricks_16_4_small_general_fleet_xlarge_single_node
  databricks_conn_id: databricks_new
