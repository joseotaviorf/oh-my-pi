# Schema validation file for core_region.business_unit_region
# Path: dags/core/core_region/schemas/core/business_unit_region.yml

columns:
  # Surrogate key - primary identifier
  sk_core_business_unit_region:
    type: string
    required: true

  # Region identifier (composite grain)
  id_region:
    type: bigint
    required: true

  # Business unit (Hub) identifier (composite grain)
  id_business_unit:
    type: bigint
    required: true

  # Business context served by the association (SALE/RENT)
  business_context:
    type: string
    required: true

  # Association creation timestamp (current source created_at)
  ts_business_unit_region_created:
    type: timestamp
    required: true

  # Association last-update timestamp (MAX CDC transaction time)
  ts_business_unit_region_updated:
    type: timestamp
    required: true

  # Load timestamp
  ts_load:
    type: timestamp
    required: true

  # Partitioning columns
  year:
    type: int
    required: true

  month:
    type: int
    required: true

  day:
    type: int
    required: true

# Schema constraints
min_columns: 10
max_columns: 10
strict: true
