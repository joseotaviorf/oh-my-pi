---
database_name: datalake_vespucio_service_clean
table_name: publisher
description:
  Table that store the publisher status from Vespucio Service Application.
  It is an append only table.
domain: Atlas DB
owner: gabriel.mota@quintoandar.com.br
columns:
  id:
    lineage:
      - datalake_vespucio_service_raw.publisher.id
    description: "The table primary key and row identification."
  id_portal:
    lineage:
      - datalake_vespucio_service_raw.publisher.portal_id
    description: "The portal identification userd by the publisher."
  id_publisher:
    lineage:
      - datalake_vespucio_service_raw.publisher.publisher_id
    description: "The publisher identification."
  listing_status:
    lineage:
      - datalake_vespucio_service_raw.publisher.listing_status
    description: "Current publication status of the listing in the Vespucio Service (e.g. active, paused, removed), indicating whether the listing is visible to buyers or renters on partner portals."
  ts_created:
    lineage:
      - datalake_vespucio_service_raw.publisher.created_at
    description: "Publisher status creation timestamp."
