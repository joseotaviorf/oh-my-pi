database_name: ops_finance
table_name: test_mat_02
owner: mario.abreu@quintoandar.com.br
domain: Data Ops & Governance
description: Table `test_mat_02` materialized on a schedule by Luigi from a user-provided SQL query (Ops Finance domain).
columns:
  dag_name:
    description: Column `dag_name` produced by the scheduled materialization query.
  dag_location:
    description: Column `dag_location` produced by the scheduled materialization query.
  current_date_ref:
    description: Column `current_date_ref` produced by the scheduled materialization query.
  previous_date_ref:
    description: Column `previous_date_ref` produced by the scheduled materialization query.
  change_status:
    description: Column `change_status` produced by the scheduled materialization query.
