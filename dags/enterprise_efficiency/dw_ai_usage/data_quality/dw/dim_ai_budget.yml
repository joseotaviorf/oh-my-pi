---
table_name: dw_ai_usage.dim_ai_budget
alert_channel: PEOPLE_ALERTS

table_level_validations:
  has_size:
    greater_than: 1
    severity_level: Error
  has_size_variation:
    variation_type: percentage
    greater_than_or_equal_to: -10
    less_than_or_equal_to: 25
    severity_level: Warning

column_level_validations:
  sk_ai_budget_version:
    is_complete:
      severity_level: Error
    is_unique:
      severity_level: Error
  sk_ai_user:
    has_null_count:
      less_than_or_equal_to: 1
      severity_level: Warning
  one_current_row_per_tool_sk_ai_user_period_type:
    custom:
      # SCD2 integrity: at most one is_current row per (tool, sk_ai_user, period_type).
      constraint: |-
        (SELECT COUNT(*) FROM (
          SELECT
            tool,
            sk_ai_user,
            period_type
          FROM dw_ai_usage.dim_ai_budget
          WHERE is_current
            AND sk_ai_budget_version != '-1'
          GROUP BY
            tool,
            sk_ai_user,
            period_type
          HAVING COUNT(*) > 1
        )) = 0
      severity_level: Warning
  no_unresolved_sk_ai_user:
    custom:
      # Every row must resolve to dim_ai_user (no -1 unknown-member FK).
      constraint: |-
        (SELECT COUNT(*)
         FROM dw_ai_usage.dim_ai_budget
         WHERE sk_ai_budget_version != '-1'
           AND sk_ai_user = -1) = 0
      severity_level: Warning
  matching_sk_ai_user_dim_ai_user:
    custom:
      # Every sk_ai_user > -1 must exist in dw_ai_usage.dim_ai_user (no orphan FK).
      constraint: |-
        (SELECT COUNT(*)
         FROM (
           SELECT DISTINCT sk_ai_user
           FROM dw_ai_usage.dim_ai_budget
           WHERE sk_ai_user > -1
         ) AS budget_user
         WHERE NOT EXISTS (
           SELECT 1
           FROM dw_ai_usage.dim_ai_user AS user_dim
           WHERE user_dim.sk_ai_user = budget_user.sk_ai_user
         )
        ) = 0
      severity_level: Warning
