---
database_name: datalake_tof_taxonomy
table_name: amplitude_user_merge
description: This table consolidates user merge data from Amplitude to provide a unified view of user identities for taxonomy enrichment.
domain: Growth
owner: fabio.soeiro@quintoandar.com.br
columns:
  id_amplitude_merged:
    description: "Unique identifier for the user in Amplitude analytics across all tracked page events after applying user merge rules."
    lineage:
      - datalake_online_attribution.events_exploded.id_amplitude
  id_user:
    description: "User identifier linking events across different pages in the user journey."
    lineage:
      - datalake_online_attribution.events_exploded.id_user
  id_amplitude_all:
    description: "The Amplitude ID considering both merged and non-merged users."
    lineage:
      - datalake_amplitude_clean.170698_user_merge.id_amplitude_merged
      - datalake_online_attribution.events_exploded.id_amplitude
