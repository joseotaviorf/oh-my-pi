---
table_name: dw_brokers.dim_broker_account_manager_history
alert_channel: GROWTH_ALERTS
table_level_validations:
  has_size:
    greater_than: 0
    severity_level: Error

column_level_validations:
  sk_broker_account_manager_history:
    is_complete:
      severity_level: Error
    is_unique:
      severity_level: Error
  sk_broker:
    is_complete:
      severity_level: Error
  has_3p_access_control:
    is_complete:
      severity_level: Error
  ts_load:
    is_complete:
      severity_level: Error
  year:
    is_complete:
      severity_level: Error
  month:
    is_complete:
      severity_level: Error
  day:
    is_complete:
      severity_level: Error
  ts_start:
    is_complete:
      severity_level: Error
  sk_user_account_manager:
    is_complete:
      severity_level: Error
  account_manager:
    custom:
      # Ensure account_manager emails belong to the @quintoandar domain (NULLs are ignored — no owner mapping).
      constraint: |-
        (SELECT COUNT(*)
         FROM dw_brokers.dim_broker_account_manager_history
         WHERE account_manager IS NOT NULL
           AND LOWER(account_manager) NOT LIKE '%@quintoandar.%'
        ) = 0
      severity_level: Warning
  business_context:
    is_complete:
      severity_level: Error
  version:
    is_complete:
      severity_level: Error
  is_current:
    is_complete:
      severity_level: Error
    custom:
      constraint: |-
        (SELECT COUNT(*) FROM (
          SELECT
            sk_broker,
            business_context,
            COUNT(*) AS cnt
          FROM
            dw_brokers.dim_broker_account_manager_history
          WHERE
            is_current
          GROUP BY
            sk_broker,
            business_context
          HAVING
            COUNT(*) > 1
        ) AS dup_current) = 0
      severity_level: Error
