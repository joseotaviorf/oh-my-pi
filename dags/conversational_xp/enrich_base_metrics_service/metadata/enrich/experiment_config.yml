database_name: datalake_search
table_name: experiment_config
owner: pedro.nogueira@quintoandar.com.br
domain: Conversational XP
description: |
  This table has the configuration of the experiments.
columns:
  experiment_name:
    description: Human-readable identifier for the experiment, used as the primary lookup key across configuration records.
  config:
    description: The config json of the experiment.
