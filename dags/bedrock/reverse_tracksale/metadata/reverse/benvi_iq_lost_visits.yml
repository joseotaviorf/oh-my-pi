---
database_name: datalake_tracksale_reverse
table_name: benvi_iq_lost_visits
columns:
  customer_name:
    lineage:
      - dw_public.dim_user.nome
  customer_email:
    lineage:
      - dw_public.dim_user.email
  customer_phone:
    lineage:
      - dw_public.dim_user.telefone_principal
  customer_cpf:
    lineage:
      - dw_public.dim_user.cpf
  id_user:
    lineage:
      - dw_public.dim_user.sk_user
  id_driver:
    lineage:
      - dw_datamarts.talk_to_agent.house_id
      - dw_datamarts.talk_to_agent.tenant_id
