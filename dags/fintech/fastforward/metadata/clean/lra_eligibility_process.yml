database_name: datalake_fastforward_clean
table_name: lra_eligibility_process
description:
  One row per lra eligibility process in the Fintech clean layer, sourced from
  datalake_fastforward_clean for payments, billing, and financial operations
  reporting.
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
columns:
  id:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.id
    description: Financial attribute representing id on the lra_eligibility_process dataset.
  id_contract_external:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.contract_external_id
    description: Identifier for the contract external referenced by this financial record.
  option_bucket:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.option_bucket
    description: Financial attribute representing option bucket on the lra_eligibility_process dataset.
  file_name:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.file_name
    description: Financial attribute representing file name on the lra_eligibility_process dataset.
  status:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.status
    description: Current lifecycle status of the financial entity in the upstream operational system.
  status_reason:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.status_reason
    description: Financial attribute representing status reason on the lra_eligibility_process dataset.
  is_eligible:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.is_eligible
    description: Indicates whether eligible applies to this financial record.
  ts_created:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.created_at
    description: Timestamp when created occurred in the upstream fintech system.
  ts_updated:
    lineage:
      - datalake_fastforward_raw.lra_eligibility_process.updated_at
    description: Timestamp when updated occurred in the upstream fintech system.
