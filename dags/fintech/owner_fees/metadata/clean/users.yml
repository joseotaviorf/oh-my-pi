database_name: datalake_owner_fees_clean
table_name: users
description:
  One row per users in the Fintech clean layer, sourced from
  datalake_owner_fees_clean for payments, billing, and financial operations
  reporting.
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
columns:
  id:
    lineage:
      - datalake_owner_fees_raw.users.id
    description: Financial attribute representing id on the users dataset.
  id_external:
    lineage:
      - datalake_owner_fees_raw.users.external_id
    description: Identifier for the external referenced by this financial record.
  user_name:
    lineage:
      - datalake_owner_fees_raw.users.name
    description: Financial attribute representing user name on the users dataset.
  user_email:
    lineage:
      - datalake_owner_fees_raw.users.email
    description: Financial attribute representing user email on the users dataset.
  user_phone:
    lineage:
      - datalake_owner_fees_raw.users.phone
    description: Financial attribute representing user phone on the users dataset.
  ts_created:
    lineage:
      - datalake_owner_fees_raw.users.created_at
    description: Timestamp when created occurred in the upstream fintech system.
  ts_updated:
    lineage:
      - datalake_owner_fees_raw.users.updated_at
    description: Timestamp when updated occurred in the upstream fintech system.
  year:
    lineage:
      - datalake_owner_fees_raw.users.year
    description: Year partition derived from the source event timestamp for incremental loading.
  month:
    lineage:
      - datalake_owner_fees_raw.users.month
    description: Month partition derived from the source event timestamp for incremental loading.
  day:
    lineage:
      - datalake_owner_fees_raw.users.day
    description: Day partition derived from the source event timestamp for incremental loading.
