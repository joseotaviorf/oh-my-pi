database_name: dw_velo
table_name: dim_velo_propose_company
description: |
  QuintoCred is the name given to Velo after its acquisition by QuintoAndar in 2021. As of 2023, the Velo brand was replaced by QuintoCred.
  In June 2025, QuintoCred's core operations were shut down, leaving only the debt collection services active. In the first quarter of 2026, the decision was made to terminate the collection operations by July 31, 2026.
  Therefore, it is important to understand that all Velo and QuintoCred systems are considered legacy, and consequently, the involved tables are as well.

  Corporate entity (PJ) registered on a QuintoCred guarantee proposal when the
  rental contract is signed by a company rather than a natural person.
domain: Fintech
owner: guilherme.sacramento@quintoandar.com.br
columns:
  sk_company:
    lineage:
    - datalake_velo.propose_company.id_company
    description: Company's identification number (ID) when the propose is for a business
  company_name:
    lineage:
    - datalake_velo.propose_company.company_name
    description: Company's name when the propose is for a business
  street:
    lineage:
      - datalake_velo.propose_company.street
    description: Company's street when the propose is for a business
  number:
    lineage:
      - datalake_velo.propose_company.number
    description: Company's address number when the propose is for a business
  complement:
    lineage:
      - datalake_velo.propose_company.complement
    description: Company's address complement when the propose is for a business
  neighborhood:
    lineage:
      - datalake_velo.propose_company.neighborhood
    description: Company's neighborhood when the propose is for a business
  city:
    lineage:
    - datalake_velo.propose_company.city
    description: Company's city when the propose is for a business
  state:
    lineage:
    - datalake_velo.propose_company.state
    description: Company's state when the propose is for a business
  country_code:
    lineage:
    - datalake_velo.propose_company.country_code
    description: Company's country when the propose is for a business
  zipcode:
    lineage:
    - datalake_velo.propose_company.zipcode
    description: Company's zipcode when the propose is for a business
  cnpj:
    lineage:
    - datalake_velo.propose_company.cnpj
    description: Company's document number (CNPJ) when the propose is for a business
  is_legacy:
    lineage:
    - datalake_velo.propose_company.is_legacy
    description: If the propose's comes from Velo 2.0 systems (TRUE = 2.0, FALSE = QuintoCred;3.0)
  ts_created:
    lineage:
    - datalake_velo.propose_company.ts_created
    description: Moment (timestamp) when the company was created
  ts_updated:
    lineage:
    - datalake_velo.propose_company.ts_updated
    description: Moment (timestamp) when the company was last updated
  ts_load:
    description: Moment (timestamp) when the table was last loaded
