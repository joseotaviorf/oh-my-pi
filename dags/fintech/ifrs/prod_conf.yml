---
ecl_asset_runs:
  source_root_path: s3://artifacts.s3.data.quintoandar.com.br/quintoml/batch/provision-assets-inference/prod-adhoc/provision_ecl_asset
  format: delta
  partition_cols:
    - year
    - month
    - day
  extraction_type: incremental
ecl_liability_runs:
  source_root_path: s3://artifacts.s3.data.quintoandar.com.br/quintoml/batch/provision-assets-inference/prod-adhoc/provision_ecl_liability
  format: delta
  partition_cols:
    - year
    - month
    - day
  extraction_type: incremental
parameter_rule:
  source_root_path: s3://artifacts.s3.data.quintoandar.com.br/quintoml/batch/provision-assets-inference/prod-adhoc/provision_parameter_rule
  format: delta
  partition_cols: []
  extraction_type: full
