---
database_name: datalake_velo_clean
table_name: clientes_address
owner: guilherme.sacramento@quintoandar.com.br
domain: Fintech
description: |
  QuintoCred is the name given to Velo after its acquisition by QuintoAndar in 2021. As of 2023, the Velo brand was replaced by QuintoCred.
  In June 2025, QuintoCred's core operations were shut down, leaving only the debt collection services active. In the first quarter of 2026, the decision was made to terminate the collection operations by July 31, 2026.
  Therefore, it is important to understand that all Velo and QuintoCred systems are considered legacy, and consequently, the involved tables are as well.
  Clean postal addresses for Velo/QuintoCred customers. Grain: one row per address record. Consumed for legacy billing and rental-guarantee reconciliation.
columns:
  id:
    description: "Primary key of the address in the legacy Velo clientes module."
    lineage:
      - datalake_velo_raw.clientes_address.id
  id_user_insert:
    description: "Velo user id that created the row in the source application."
    lineage:
      - datalake_velo_raw.clientes_address.userinsert
  id_user_update:
    description: "Velo user id that last updated the row in the source application."
    lineage:
      - datalake_velo_raw.clientes_address.userupdate
  neighborhood:
    description: "Neighborhood (bairro) of the customer address as captured in Velo."
    lineage:
      - datalake_velo_raw.clientes_address.neighborhood
  street:
    description: "Street name of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.street
  number:
    description: "Street number of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.number
  complement:
    description: "Address complement (apartment, block, reference) when provided."
    lineage:
      - datalake_velo_raw.clientes_address.complement
  zipcode:
    description: "Postal code (CEP) of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.zipcode
  city:
    description: "City of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.city
  state:
    description: "State (UF) of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.state
  country:
    description: "Country of the customer address."
    lineage:
      - datalake_velo_raw.clientes_address.country
  geolocation:
    description: "Geographic coordinates or geolocation string stored by Velo for the address."
    lineage:
      - datalake_velo_raw.clientes_address.geolocation
  is_active:
    description: "True when the address is marked active in the Velo source system."
    lineage:
      - datalake_velo_raw.clientes_address.active
  ts_inserted:
    description: "Timestamp when the address row was first inserted in Velo."
    lineage:
      - datalake_velo_raw.clientes_address.dateinsert
  ts_updated:
    description: "Timestamp of the last update to the address row in Velo."
    lineage:
      - datalake_velo_raw.clientes_address.dateupdate
