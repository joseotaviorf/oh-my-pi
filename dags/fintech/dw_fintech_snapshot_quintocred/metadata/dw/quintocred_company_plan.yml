---
database_name: dw_fintech_snapshot_quintocred
table_name: quintocred_company_plan
description:
  QuintoCred is the name given to Velo after its acquisition by QuintoAndar in 2021. As of 2023, the Velo brand was replaced by QuintoCred.
  In June 2025, QuintoCred's core operations were shut down, leaving only the debt collection services active. In the first quarter of 2026, the decision was made to terminate the collection operations by July 31, 2026.
  Therefore, it is important to understand that all Velo and QuintoCred systems are considered legacy, and consequently, the involved tables are as well.
  One row per quintocred company plan in the QuintoCred rental guarantee
  product, tracking underwriting, charges, renewals, and collections for
  landlord protection policies.
owner: guilherme.sacramento@quintoandar.com.br
domain: Fintech
columns:
  id:
    description: "Unique identifier (ID) for the company plan record"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.id

  id_plan:
    description: "Identifier (ID) of the associated plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.id_plan

  id_company:
    description: "Identifier (ID) of the company linked to the plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.id_company

  pricing:
    description: "Pricing value associated with the company plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.pricing

  coverage:
    description: "Coverage value defined in the company plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.coverage

  damage:
    description: "Damage coverage value in the company plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.damage

  commission:
    description: "Commission value associated with the company plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.commission

  version:
    description: "Version number of the company plan"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.version

  is_active:
    description: "Flag indicating whether the company plan is active"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.is_active

  ts_created:
    description: "Timestamp when the company plan record was created"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.ts_created

  ts_updated:
    description: "Timestamp when the company plan record was last updated"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.ts_updated

  ts_snapshot:
    description: "Timestamp when the snapshot of the data was taken"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.NOW()

  year:
    description: "Year when the snapshot was taken"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.YEAR(CURRENT_DATE)

  month:
    description: "Month when the snapshot was taken"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.MONTH(CURRENT_DATE)

  day:
    description: "Day of the month when the snapshot was taken"
    lineage:
      - datalake_rental_guarantee_platform_clean.company_plan.DAY(CURRENT_DATE)
