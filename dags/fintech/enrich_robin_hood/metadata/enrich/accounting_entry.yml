---
database_name: datalake_robin_hood
table_name: accounting_entry
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
description: Accounting entry enriched data from Robin Hood.
columns:
  id:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.id
    description: Financial attribute representing id on the accounting_entry dataset.
  id_external:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.id_external
    description: External identifier for the accounting entry in upstream systems.
  id_source:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.id_source
    description: Internal source identifier linking the entry to accounting_entry_source.
  id_payee:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.id_payee
    description: Payee identifier for the party receiving the accounting entry payment.
  id_payee_external:
    lineage:
      - datalake_robin_hood_clean.payee.id_payee.id_external
    description: Identifier for the payee external referenced by this financial record.
  id_lead:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.metadata
    description: Lead identifier parsed from accounting entry metadata when present.
  id_house:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.metadata
    description: House identifier parsed from accounting entry metadata when present.
  id_contract:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.metadata
    description: Contract identifier parsed from accounting entry metadata when present.
  id_offer:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.metadata
    description: Offer identifier parsed from accounting entry metadata when present.
  cost_center_code:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.cost_center_code
    description: Financial attribute representing cost center code on the accounting_entry dataset.
  source_bill_item:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.source_bill_item
    description: Financial attribute representing source bill item on the accounting_entry dataset.
  source_code:
    lineage:
      - datalake_robin_hood_clean.accounting_entry_source.code
    description: Accounting entry source code from accounting_entry_source (e.g. ciq-listing-purchase).
  source_name:
    lineage:
      - datalake_robin_hood_clean.accounting_entry_source.source_name
    description: Human-readable accounting entry source name from accounting_entry_source.
  description:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.description
    description: Free-text description of the accounting entry from Robin Hood.
  type:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.type
    description: Financial attribute representing type on the accounting_entry dataset.
  locale:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.locale
    description: Locale or market code associated with the accounting entry.
  due_amount:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.due_amount
    description: Monetary amount due for this accounting entry.
  accrual_year_month:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.accrual_year_month
    description: Financial attribute representing accrual year month on the accounting_entry dataset.
  accounting_year_month:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.accounting_year_month
    description: Financial attribute representing accounting year month on the accounting_entry dataset.
  dt_occurrence:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.dt_occurrence
    description: Calendar date associated with occurrence in the financial workflow.
  ts_blocked:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.ts_blocked
    description: Timestamp when blocked occurred in the upstream fintech system.
  ts_created:
    lineage:
      - datalake_robin_hood_clean.accounting_entry.ts_created
    description: Creation timestamp.
