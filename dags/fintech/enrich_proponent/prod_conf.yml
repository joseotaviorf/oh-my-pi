---
custom_cluster:
  driver_node_type_id: m5d.2xlarge
  node_type_id: m5d.2xlarge
  # runtime_engine: PHOTON
  autoscale:
    min_workers: 1
    max_workers: 3
  spark_conf:
    spark.driver.maxResultSize: "0"
inner_dependencies:
  main_proponent_proposal:
    - proponent
incremental_tables:
partition_cols:
