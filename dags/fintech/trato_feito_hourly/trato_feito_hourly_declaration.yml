dag:
  name: trato_feito_hourly
  schedule_interval: 0 8-21 * * *
  owner: Data Fintech
  documentation:
    dag_purpose: |
      [Trato Feito](https://github.com/quintoandar/trato-feito) manages negotiations with defaulting
      clients — negotiation objects, conditions, statuses, and the installments/payments they generate.
      This hourly CDC DAG (`0 8-21 * * *`, 08:00–21:00) gives Collections near-real-time data on
      negotiations made and installment payments.
workflow:
  type: cdc
  layer: raw
  database_type: postgres
  source_schema: public
  dbutils_secret_key: TRATOFEITO_DB
  lineage_product_database_name: tratofeito
  source_database: tratofeito
  default_extraction_type: incremental
  default_partitions: [year, month, day]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, macros.ds_add(data_interval_start | ds, 1), 'load_end_date') }}"
  tables_customization:
    installment: {}
    negotiation: {}
    payment: {}
