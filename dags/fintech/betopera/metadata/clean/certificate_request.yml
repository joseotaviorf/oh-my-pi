database_name: datalake_betopera_clean
table_name: certificate_request
description:
  One row per certificate request in the Fintech clean layer, sourced from
  datalake_betopera_clean for payments, billing, and financial operations
  reporting.
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
columns:
  id:
    lineage:
      - datalake_betopera_raw.certificate_request.id
    description: Financial attribute representing id on the certificate_request dataset.
  id_insurance:
    lineage:
      - datalake_betopera_raw.certificate_request.insurance_id
    description: Identifier for the insurance referenced by this financial record.
  version:
    lineage:
      - datalake_betopera_raw.certificate_request.version
    description: Financial attribute representing version on the certificate_request dataset.
  payload:
    lineage:
      - datalake_betopera_raw.certificate_request.payload
    description: Financial attribute representing payload on the certificate_request dataset.
  status:
    lineage:
      - datalake_betopera_raw.certificate_request.status
    description: Current lifecycle status of the financial entity in the upstream operational system.
  ts_synced:
    lineage:
      - datalake_betopera_raw.certificate_request.synced_at
    description: Timestamp when synced occurred in the upstream fintech system.
  ts_created:
    lineage:
      - datalake_betopera_raw.certificate_request.created_at
    description: Timestamp when created occurred in the upstream fintech system.
  ts_updated:
    lineage:
      - datalake_betopera_raw.certificate_request.updated_at
    description: Timestamp when updated occurred in the upstream fintech system.
  year:
    lineage:
      - datalake_betopera_raw.certificate_request.year
    description: Year partition derived from the source event timestamp for incremental loading.
  month:
    lineage:
      - datalake_betopera_raw.certificate_request.month
    description: Month partition derived from the source event timestamp for incremental loading.
  day:
    lineage:
      - datalake_betopera_raw.certificate_request.day
    description: Day partition derived from the source event timestamp for incremental loading.
