---
dag:
  name: enrich_invoice_preview
  schedule_start_date: 2020, 11, 1
  owner: Data Fintech
  documentation:
    dag_purpose: |
      Stakeholder-ready invoice preview snapshot from SeuBarriga. Applies sign convention
      on amounts, renames blocked, derives payment_installment, and fully refreshes a
      single Brazil snapshot from the latest load partition day and morning export_slot
      batch only (afternoon clean loads are not published here). Ignores older partition
      reprocessing. Historical loads remain in datalake_invoice_preview_clean.invoice_preview.
      Triggered after invoice_preview clean loads (09:00 and 16:00 BRT slots).
workflow:
  type: query_delta
  layer: enrich
  tables_customization:
    invoice_preview:
      extraction_type: full
