---
database_name: datalake_velo
table_name: omie_client
owner: guilherme.sacramento@quintoandar.com.br
domain: Fintech
description:
  QuintoCred is the name given to Velo after its acquisition by QuintoAndar in 2021. As of 2023, the Velo brand was replaced by QuintoCred.
  In June 2025, QuintoCred's core operations were shut down, leaving only the debt collection services active. In the first quarter of 2026, the decision was made to terminate the collection operations by July 31, 2026.
  Therefore, it is important to understand that all Velo and QuintoCred systems are considered legacy, and consequently, the involved tables are as well.
  Table containing Omie clients information
columns:
  id_client:
    lineage:
      - datalake_velo_omie_clean.clients.id_client
    description: Omie Client's internal identification number 
  corporate_name:
    lineage:
      - datalake_velo_omie_clean.clients.corporate_name
    description: Client's name or Client's official Corporate Name.
  name_doing_business_as:
    lineage:
      - datalake_velo_omie_clean.clients.name_doing_business_as
    description: Client's name or Client's Trade Name.
  client_cpf_cnpj:
    lineage:
      - datalake_velo_omie_clean.clients.client_cpf_cnpj
    description: Client's document, being CPF or CNPJ.
  email:
    lineage:
      - datalake_velo_omie_clean.clients.email
    description: Velo platform attribute describing email for condominium billing reconciliation.
  address:
    lineage:
      - datalake_velo_omie_clean.clients.address
    description: Client's main address info/street.
  address_number:
    lineage:
      - datalake_velo_omie_clean.clients.address_number
    description: Client's main address number.
  address_complement:
    lineage:
      - datalake_velo_omie_clean.clients.address_complement
    description: Client's main address complement.
  neighborhood:
    lineage:
      - datalake_velo_omie_clean.clients.neighborhood
    description: Client's main address neighborhood.
  city:
    lineage:
      - datalake_velo_omie_clean.clients.city
    description: Client's main address city.
  state:
    lineage:
      - datalake_velo_omie_clean.clients.state
    description: Client's main address state.
  country_code:
    lineage:
      - datalake_velo_omie_clean.clients.country_code
    description: Client's main address country code following quintoandar's pattern.
  is_natural_person:
    lineage:
      - datalake_velo_omie_clean.clients.is_natural_person
    description: If the client is a natural person or a company.
  is_foreign:
    lineage:
      - datalake_velo_omie_clean.clients.is_foreign
    description: If this client if foreign.
  is_active:
    description: If this client is active.