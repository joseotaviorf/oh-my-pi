---
database_name: datalake_checkout_clean
table_name: credit_card_acquire_fee
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
description: One row per credit card acquire fee in the Fintech clean layer, sourced from datalake_checkout_clean for payments, billing, and financial operations reporting.
columns:
  id:
    description: Financial attribute representing id on the credit_card_acquire_fee dataset.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.id
  acquire_name:
    description: Financial attribute representing acquire name on the credit_card_acquire_fee dataset.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.acquire_name
  card_brand:
    description: Financial attribute representing card brand on the credit_card_acquire_fee dataset.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.card_brand
  fee_percentage:
    description: Financial attribute representing fee percentage on the credit_card_acquire_fee dataset.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.fee_percentage
  installments:
    description: Financial attribute representing installments on the credit_card_acquire_fee dataset.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.installments
  ts_created:
    description: Timestamp when created occurred in the upstream fintech system.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.created_at
  ts_disabled:
    description: Timestamp when disabled occurred in the upstream fintech system.
    lineage:
    - datalake_checkout_raw.credit_card_acquire_fee.disabled_at
