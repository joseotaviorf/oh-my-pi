---
database_name: datalake_checkout_clean
table_name: boleto_webhook
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
description: One row per boleto webhook in the Fintech clean layer, sourced from datalake_checkout_clean for payments, billing, and financial operations reporting.
columns:
  id:
    description: Financial attribute representing id on the boleto_webhook dataset.
    lineage:
    - datalake_checkout_raw.boleto_webhook.id
  id_bolecode:
    description: Identifier for the bolecode referenced by this financial record.
    lineage:
    - datalake_checkout_raw.boleto_webhook.bolecode_id
  id_boleto:
    description: Identifier for the boleto referenced by this financial record.
    lineage:
    - datalake_checkout_raw.boleto_webhook.boleto_id
  status:
    description: Current lifecycle status of the financial entity in the upstream operational system.
    lineage:
    - datalake_checkout_raw.boleto_webhook.status
  error_message:
    description: Financial attribute representing error message on the boleto_webhook dataset.
    lineage:
    - datalake_checkout_raw.boleto_webhook.error_message
  payload:
    description: Financial attribute representing payload on the boleto_webhook dataset.
    lineage:
    - datalake_checkout_raw.boleto_webhook.payload
  ts_created:
    description: Timestamp when created occurred in the upstream fintech system.
    lineage:
    - datalake_checkout_raw.boleto_webhook.TIMESTAMP(created_at)
  ts_updated:
    description: Timestamp when updated occurred in the upstream fintech system.
    lineage:
    - datalake_checkout_raw.boleto_webhook.TIMESTAMP(updated_at)
