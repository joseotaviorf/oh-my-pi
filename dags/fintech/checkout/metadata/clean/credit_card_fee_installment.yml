---
database_name: datalake_checkout_clean
table_name: credit_card_fee_installment
description:
  One row per credit card fee installment in the Fintech clean layer, sourced
  from datalake_checkout_clean for payments, billing, and financial operations
  reporting.
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
columns:
  id:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.id
    description: Financial attribute representing id on the credit_card_fee_installment dataset.
  id_credit_card_fee:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.credit_card_fee_id
    description: Identifier for the credit card fee referenced by this financial record.
  installments:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.installments
    description: Financial attribute representing installments on the credit_card_fee_installment dataset.
  convenience_fee_percentage:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.convenience_fee_percentage
    description: Financial attribute representing convenience fee percentage on the credit_card_fee_installment dataset.
  fee_percentage:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.fee_percentage
    description: Financial attribute representing fee percentage on the credit_card_fee_installment dataset.
  ts_created:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.created_at
    description: Timestamp when created occurred in the upstream fintech system.
  ts_disabled:
    lineage:
    - datalake_checkout_raw.credit_card_fee_installment.disabled_at
    description: Timestamp when disabled occurred in the upstream fintech system.
