---
database_name: datalake_checkout_clean
table_name: pix_webhook
description:
  One row per pix webhook in the Fintech clean layer, sourced from
  datalake_checkout_clean for payments, billing, and financial operations
  reporting.
owner: juliana.hocihara@quintoandar.com.br
domain: Fintech
columns:
  id:
    lineage:
    - datalake_checkout_raw.pix_webhook.id
    description: Financial attribute representing id on the pix_webhook dataset.
  id_bolecode:
    lineage:
    - datalake_checkout_raw.pix_webhook.bolecode_id
    description: Identifier for the bolecode referenced by this financial record.
  id_pix:
    lineage:
    - datalake_checkout_raw.pix_webhook.pix_id
    description: Identifier for the pix referenced by this financial record.
  status:
    lineage:
    - datalake_checkout_raw.pix_webhook.status
    description: Current lifecycle status of the financial entity in the upstream operational system.
  error_message:
    lineage:
    - datalake_checkout_raw.pix_webhook.error_message
    description: Financial attribute representing error message on the pix_webhook dataset.
  payload:
    lineage:
    - datalake_checkout_raw.pix_webhook.payload
    description: Financial attribute representing payload on the pix_webhook dataset.
  ts_created:
    lineage:
    - datalake_checkout_raw.pix_webhook.created_at
    description: Timestamp when created occurred in the upstream fintech system.
  ts_updated:
    lineage:
    - datalake_checkout_raw.pix_webhook.updated_at
    description: Timestamp when updated occurred in the upstream fintech system.
