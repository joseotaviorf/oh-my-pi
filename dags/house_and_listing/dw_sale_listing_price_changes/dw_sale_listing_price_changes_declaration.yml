dag:
  name: dw_sale_listing_price_changes
  owner: Data House and Listing
workflow:
  type: query_delta
  layer: dw
  custom_schema: sale
  inner_dependencies:
    fact_listing_price_changes:
      - dim_sale_price_segment
