---
database_name: datalake_ebdb_clean
table_name: listing_sale_model
owner: bruna.prates@quintoandar.com.br
domain: House and Listing
description: CDC mirror of the ListingSaleModel EBDB table. One row per sale model record (PK id).
  Stores sale-specific attributes for each listing business context, including primary market flag and great sale price tag.
columns:
  id:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.id
    description: Primary key identifier for the listing sale model record.
  id_listing_business_context:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.listingBusinessContext_id
    description: Foreign key identifier referencing the listing business context.
  is_primary_market:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.isprimarymarket
    description: Legacy upstream boolean from ListingSaleModel. Not authoritative and not used for current market classification; use sale_type instead.
  has_great_sale_price_tag:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.hasGreatSalePriceTag
    description: Boolean flag indicating if the listing has a great sale price tag.
  ts_created:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.created_at
    description: Timestamp when this record was created.
  ts_updated:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.updated_at
    description: Timestamp of the last update to this record.
  op_cdc:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.op_cdc
    description: CDC operation type captured by Debezium.
    categories:
      r: Read/snapshot - initial load of existing records.
      c: Create - new record inserted.
      u: Update - existing record modified.
      d: Delete - record removed.
  ts_cdc_transaction:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.ts_cdc_transaction
    description: Timestamp when the CDC system captured this transaction (millisecond precision).
  ts_database_transaction:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.ts_database_transaction
    description: Timestamp of the original database transaction in the source system.
  sale_type:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.saleType
    description: Enum discriminator indicating the market type. PRIMARY for new builds, SECONDARY for resale. Defaults to SECONDARY.
  unit_count:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.unitCount
    description: Number of available units. Populated for PRIMARY market listings only.
  min_price:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.minPrice
    description: Minimum price of the typology range in BRL. Populated for PRIMARY market listings only.
  max_price:
    lineage:
      - datalake_ebdb_raw.listingsalemodel.maxPrice
    description: Maximum price of the typology range in BRL. Populated for PRIMARY market listings only.
