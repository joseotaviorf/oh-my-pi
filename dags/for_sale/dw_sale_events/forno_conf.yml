---
tables:
  - table_name: dim_sale_event_type
    is_incremental: false
  - table_name: fact_sale_demand_event
    is_incremental: true
    partitions:
      - year
      - month
      - day
    dw_query_filters:
      "year" : "{year}"
      "month" : "{month}"
      "day" : "{day}"