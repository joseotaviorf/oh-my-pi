---
dag:
  name: enrich_buyer_prospect
  schedule_start_date: 2020, 7, 1
  owner: Data ForSale
  documentation:
    dag_purpose: This DAG creates the enriched table for buyer prospect context.
workflow:
  type: query_delta
  layer: enrich
  extra_query_template_params:
    # bp_window_days: NULL = infinite lookback (all-time). Set an integer
    # (e.g. 90) to only count Primary/Secondary touches within that many
    # days of load_start_date when classifying buyer_prospect_type.bp_market_type.
    bp_window_days: "NULL"
