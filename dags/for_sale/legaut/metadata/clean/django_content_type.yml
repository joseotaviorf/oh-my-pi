---
database_name: datalake_legaut_clean
table_name: django_content_type
description: Django content types from Legaut used to resolve which model an audit log row refers to.
domain: For Sale
owner: wesley.gomes@quintoandar.com.br
columns:
  id:
    lineage:
      - datalake_legaut_raw.django_content_type.id
    description: Primary key of the content type row.
  app_label:
    lineage:
      - datalake_legaut_raw.django_content_type.app_label
    description: Django app label for the model (e.g. meusite).
  content_model:
    lineage:
      - datalake_legaut_raw.django_content_type.model
    description: Lowercase model name in Django (e.g. unit).
  ts_load:
    description: Ingestion timestamp when this row was written to the clean layer.
