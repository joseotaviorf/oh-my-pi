---
dag:
  name: enrich_sale_ongoing_listings
  schedule_start_date: 2021, 1, 1
  owner: Data ForSale
  documentation:
    dag_purpose: This DAG uploads a periodic snapshot table about ForSale ongoing listings.
      DO NOT PERFORM HISTORICAL REPROCESSING PRIOR TO JUNE 15, 2025. Data before this date was obtained through the fact table,
      whose code is not the same.
workflow:
  type: query_delta
  layer: enrich
  default_extraction_type: incremental
  default_partitions: [ "year", "month", "day" ]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_end_date') }}"
cluster:
  type: databricks_13_3_med_memory_2_workers_general_cluster
  databricks_conn_id: databricks_new
  custom_configurations:
    single_user_name: '{{ var.value.databricks_single_user_name }}'
    data_security_mode: SINGLE_USER
    spark_conf:
      spark.databricks.sql.initial.catalog.namespace: quintoandar_{{ var.value.environment }}
