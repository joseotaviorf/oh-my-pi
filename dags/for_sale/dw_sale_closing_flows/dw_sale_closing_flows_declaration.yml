dag:
  name: dw_sale_closing_flows
  owner: Data ForSale
  documentation:
    dag_purpose: This DAG creates the model for Sale Closing Flows.
workflow:
  type: query_delta
  layer: dw
  custom_schema: sale
