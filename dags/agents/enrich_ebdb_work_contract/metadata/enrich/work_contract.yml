---
database_name: datalake_ebdb_work_contract
table_name: work_contract
description: One row per EBDB partner work-contract template used to accredit agents. Grain is id from datalake_ebdb_clean.work_contract. Carries the contract title, a 3P partner tag parsed from that title, hub-team mapping from a historical GSheets standardization, and timestamps. Consumed by agent contract enrich/DW models and reverse Nazaré 3P demand matching.
domain: Agents
owner: gustavo.rompe@quintoandar.com.br
columns:
  id:
    description: EBDB work contract primary key identifying the partner accreditation agreement record.
    lineage:
      - datalake_ebdb_clean.work_contract.id
  id_hub_teams:
    description: ID of the business unit team.
    lineage:
      - datalake_gsheets_clean.sale_business_unit_standardization.id_business_unit_teams
  hub_name_teams:
    description: Standardized hub team label from sale business unit mapping when the contract name matches a known hub work-contract name.
    lineage:
      - datalake_gsheets_clean.sale_business_unit_standardization.hub_name_teams
  contract_name:
    description: Legal or operational contract title from EBDB work_contract, often embedding 3P partner tags in brackets.
    lineage:
      - datalake_ebdb_clean.work_contract.contract_name
  3p_partner:
    description: Third-party brokerage partner name extracted from a [3P-*] tag in the EBDB contract name. Null when the contract name has no such tag.
    lineage:
      - datalake_ebdb_clean.work_contract.contract_name
  is_3p_contract:
    description: True when the EBDB contract name contains a [3P-*] or [3p-*] tag identifying a third-party partner contract.
    lineage:
      - datalake_ebdb_clean.work_contract.contract_name
  ts_created:
    description: Timestamp which this contract was created.
    lineage:
      - datalake_ebdb_clean.work_contract.ts_created
  ts_updated:
    description: Timestamp which this contract had an update.
    lineage:
      - datalake_ebdb_clean.work_contract.ts_created
