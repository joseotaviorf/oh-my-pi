---
database_name: datalake_big_agent_clean
table_name: incentive_engine
description: |
  Incentive engine configurations, including external condition, incentive system,
  status, and classification/partner tier validity dates, plus timestamps and partitions.
domain: Agents
owner: gustavo.rompe@quintoandar.com.br
columns:
  id:
    lineage:
      - datalake_big_agent_raw.incentive_engine.id
    description: Record identifier.
  id_external_condition:
    lineage:
      - datalake_big_agent_raw.incentive_engine.external_condition_id
    description: External condition identifier.
  external_condition_type:
    lineage:
      - datalake_big_agent_raw.incentive_engine.external_condition_type
    description: Discriminator for the external rule entity linked to this incentive engine row.
  incentive_system:
    lineage:
      - datalake_big_agent_raw.incentive_engine.incentive_system
    description: Associated incentive system.
  incentive_system_configuration_key:
    lineage:
      - datalake_big_agent_raw.incentive_engine.incentive_system_configuration_key
    description: Key identifying the incentive system's configuration applied to this incentive engine row.
  status:
    lineage:
      - datalake_big_agent_raw.incentive_engine.status
    description: Lifecycle status of the incentive engine configuration (active, inactive, etc.).
  author:
    lineage:
      - datalake_big_agent_raw.incentive_engine.author
    description: Author responsible for creating the incentive engine.
  ts_validity_started:
    lineage:
      - datalake_big_agent_raw.incentive_engine.validity_start_at
    description: Start date of the validity window.
  ts_validity_ended:
    lineage:
      - datalake_big_agent_raw.incentive_engine.validity_end_at
    description: End date of the validity window.
  ts_created:
    lineage:
      - datalake_big_agent_raw.incentive_engine.created_at
    description: Record creation timestamp.
  ts_updated:
    lineage:
      - datalake_big_agent_raw.incentive_engine.updated_at
    description: Record last update timestamp.
  op_cdc:
    description: CDC operation type captured by Debezium.
    lineage:
      - datalake_big_agent_raw.incentive_engine.op_cdc
    categories:
      r: Read/snapshot - initial load of existing records.
      c: Create - new record inserted.
      u: Update - existing record modified.
      d: Delete - record removed.
  ts_cdc_transaction:
    description: Timestamp when the CDC system captured this transaction (millisecond precision).
    lineage:
      - datalake_big_agent_raw.incentive_engine.ts_cdc_transaction
  ts_database_transaction:
    description: Timestamp of the original database transaction in the source system.
    lineage:
      - datalake_big_agent_raw.incentive_engine.ts_database_transaction
  year:
    lineage:
      - datalake_big_agent_raw.incentive_engine.year
    description: Partition year.
  month:
    lineage:
      - datalake_big_agent_raw.incentive_engine.month
    description: Partition month.
  day:
    lineage:
      - datalake_big_agent_raw.incentive_engine.day
    description: Partition day.
