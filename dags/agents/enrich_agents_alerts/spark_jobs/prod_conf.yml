# Notification Hub Internal — inmetro route (per-alert ?space= from registry channel).
notification_hub_inmetro_webhook_base: "https://notification-hub-internal-api.apps.core-prd.habitat.zone/webhook/inmetro"
