---
dag:
  name: enrich_big_agent_incentives
  schedule_start_date: 2020, 7, 1
  owner: Data Agents
  documentation:
    dag_purpose: |
      Creates enriched tables for Big Agent Incentives Engine System and Earnings processing.
workflow:
  type: query_delta
  layer: enrich
  custom_schema: big_agent
  default_extraction_type: incremental
  default_partitions: [year, month, day]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, macros.ds_add(data_interval_start | ds, -1), 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_end_date') }}"
  inner_dependencies:
    earnings:
      - partner_tier
      - revenue_share
      - unresolved_earnings
    earnings_unified:
      - earnings
  tables_customization:
    earnings:
      merge_on: [id_earning, id_earning_source]
      z_order_by: [id_earning_source, id_earning]
    earnings_unified:
      merge_on: [id_earning_unified]
      z_order_by: [id_contract, ts_created]
    partner_tier:
      merge_on: [id_partner_tier]
      z_order_by: [id_partner_tier, ts_updated]
    revenue_share:
      merge_on: [id_revenue_share]
      z_order_by: [id_revenue_share, ts_created]
    tier_rule:
      merge_on: [id_tier]
      z_order_by: [id_tier, id_business_unit]
    unresolved_earnings:
      merge_on: [id_unresolved_earning]
      z_order_by: [ts_created]
