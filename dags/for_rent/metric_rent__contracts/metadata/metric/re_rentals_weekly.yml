---
database_name: metric_rent
table_name: re_rentals_weekly
description: New Rentals in houses which already had a previous contract signed.
domain: For Rent
owner: kleber.inoue@quintoandar.com.br
columns:
  week:
    lineage:
      - dw_public.dim_contract.dt_start
      - dw_public.dim_contract.dt_entrance
    description: Date when a contract started or the entrance happened.
    dimension: True
  country_code:
    lineage:
      - dw_public.dim_contract.country_code
    description: Country code of the house
    dimension: True
  re_rentals:
    description: New Rentals in houses which already had a previous contract signed.
    metric:
      name: Re Rentals Weekly
      description: New Rentals in houses which already had a previous contract signed.
        It's the count distinct of sk_contracts that were in status Ativo or Finalizado and the house already was rented before.
      acronym: RR
      is_additive: True
      business_stage: Demand
      hierarchy: User analysis
      approved_by: augusto.torres@quintoandar.com.br
      link_to_metric: 'https://quintoandar.sa.looker.com/looks/4756'

