dag:
  name: enrich_ebdb_contract
  schedule_start_date: 2020, 7, 1
  owner: Data ForRent
  criticality: High
  documentation:
    dag_purpose: |
      Creates enriched tables for the context `Contract` of ebdb.
      This DAG is triggered by Mediator and loads the table via full load.
workflow:
  type: query_delta
  layer: enrich
  default_partitions:
  - country_code
  inner_dependencies:
    contract_declined_relisting:
    - contract
