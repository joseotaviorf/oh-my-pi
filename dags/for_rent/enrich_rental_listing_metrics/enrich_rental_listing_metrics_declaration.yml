---
dag:
  name: enrich_rental_listing_metrics
  schedule_start_date: 2020, 7, 16
  owner: Data ForRent
  documentation: 
    dag_purpose: |
      Creates enriched tables that has rental listings metrics like the amount of publicated listings in a week or cohort analysis.
      Usually the date values drives the tables of this DAG. 
      This DAG is triggered by Mediator and loads the table via full load.
workflow:
  type: query
  layer: enrich
  inner_dependencies:
    cohort_listings_20w:
    - cohort_listings_weekly
cluster:
  type: databricks_12_2_med_2xlarge_general_photon_cluster
