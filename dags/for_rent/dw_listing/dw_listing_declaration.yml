dag:
  name: dw_listing
  schedule_start_date: 2021,08,02
  owner: Data ForRent
workflow:
  type: query
  layer: dw
  custom_schema: rent
  default_partitions: [country_code]
  tables_customization:
    dim_condo:
      partitions: []
      custom_schema: public
    fact_house_listing_daily_infos:
      partitions: ["country_code", "year", "month", "day"]
      extraction_type: incremental
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_house_listing_daily_available_hours:
      partitions: [ "year", "month", "day" ]
      extraction_type: incremental
      custom_schema: listing
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
cluster:
  type: databricks_12_2_med_general_photon_cluster
  access_control_list:
    group_name: analytics-engineers
    permission_level: CAN_MANAGE
