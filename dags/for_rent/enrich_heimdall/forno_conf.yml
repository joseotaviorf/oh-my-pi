---
dag_documentation:
  dag_purpose: |
    Expanding columns from `heimdall` datalake table `activity` to facilitate the analysis.

inner_dependencies:
  ongoing_refund_requests:
    - activity
    - auditable_expenses
    - expenses
