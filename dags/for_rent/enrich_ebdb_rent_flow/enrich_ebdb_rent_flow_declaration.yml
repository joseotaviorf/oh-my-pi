dag:
  name: enrich_ebdb_rent_flow
  schedule_start_date: 2020, 7, 1
  owner: Data ForRent
  criticality: High
  sla_deadline_localtime: "08:00"
  documentation:
    dag_purpose: |
      Creates enriched tables for the context `Rent Flow` of ebdb.
      This DAG is triggered by Mediator and loads the table via full load.
workflow:
  type: query_delta
  layer: enrich
