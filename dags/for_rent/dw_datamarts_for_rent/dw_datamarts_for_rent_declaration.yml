dag:
  name: dw_datamarts_for_rent
  owner: Data ForSale
  documentation:
    dag_purpose: |
      Creates/updates the datamart tables that don't have cross squad dependencies, for the context of Rent, in data lake.
workflow:
  type: query_delta
  layer: dw
  custom_schema: datamarts
