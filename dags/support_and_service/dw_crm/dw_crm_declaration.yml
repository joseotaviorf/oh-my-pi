dag:
  name: dw_crm
  schedule_start_date: 2020,11,1
  owner: Data SS
workflow:
  type: query
  layer: dw
  custom_schema: crm
  default_partitions: [year, month, day]
  default_extraction_type: full
  inner_dependencies:
    fact_lead_tasks:
      - fact_lead_task_actions
  tables_customization:
    dim_closing_task_transition:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    dim_closing_task_workflow:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_closing_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_closing_tasks_workflows_transitions:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_collection_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_credit_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_inspection_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_lead_task_actions:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_linhadireta_chat_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_offboarding_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_onboarding_tenant_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_payment_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_photo_job_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_repair_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_ungrouped_manual_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
    fact_visit_tasks:
      extraction_type: incremental
      partitions: [year, month, day]
      extra_query_template_params:
        year: "{year}"
        month: "{month}"
        day: "{day}"
cluster:
  type: databricks_10_4_med_memory_photon_cluster
  access_control_list:
    group_name: analytics-engineers
    permission_level: CAN_MANAGE
