---
database_name: dw_sauron
table_name: fact_session_tags
description: Fact table that contains events data, flags and its related foreign keys
  on each credit task grouped by task action
domain: Support and Service
owner: data-ae-ss@quintoandar.com.br
columns:
  sk_session:
    lineage:
      - datalake_sauron.session.id_session
    description: Primary key that identifies the session.
  tags:
    lineage:
      - datalake_sauron.session.customer_tags
    description: Tag identifying the user's step in the session.
  ts_load:
    description: Model execution date, in timestamp format.
