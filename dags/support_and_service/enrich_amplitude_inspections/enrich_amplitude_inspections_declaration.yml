dag:
  name: enrich_amplitude_inspections
  schedule_start_date: 2022, 1, 19
  owner: Data SS
  documentation:
    dag_purpose: |
      Formatting and json extract in Inspection Service APP data.

workflow:
  type: query_delta
  layer: enrich
  tables_customization:
    inspection_stages_access:
      partitions: [year, month, day]
      extraction_type: incremental
      z_order_by: [id_inspection]
      merge_on: [id_inspection]
  extra_query_template_params:
    load_start_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_start_date') }}"
    load_end_date: "{{ get_date_param(dag_run, data_interval_start | ds, 'load_end_date') }}"

cluster:
  type: databricks_13_3_min_io-memory_cluster
  databricks_conn_id: databricks_new
  custom_configurations:
    single_user_name: '{{ var.value.databricks_single_user_name }}'
    data_security_mode: SINGLE_USER
    spark_conf:
      spark.databricks.sql.initial.catalog.namespace: quintoandar_{{ var.value.environment }}
