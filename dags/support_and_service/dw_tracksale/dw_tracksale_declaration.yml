dag:
  name: dw_tracksale
  owner: Data SS
  documentation:
    dag_purpose: |
      This DAG loads the DW tables with [Tracksale](https://www.tracksale.co/) data. This data is related to NPS.
workflow:
  type: query_delta
  layer: dw
  custom_schema: tracksale
cluster:
  type: databricks_12_2_med_general_cluster
