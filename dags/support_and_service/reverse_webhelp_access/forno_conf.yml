database_name: reverse_webhelp
azure_container_name: lake-quinto-andar-hml

tables:
  backlog_metric:
    context: cx
  csat_front:
    context: cx
  demand_metric:
    context: cx
  fcr_metric:
    context: cx
  general_metric:
    context: cx
  listing_quality_tasks:
    context: services
  listing_quality_sla:
    context: services
  repair_tickets:
    context: services
  speech_call:
    context: speech_analytics
  speech_chat:
    context: speech_analytics
  taxonomy:
    context: cx
  recontact:
    context: cx
  tickets_transferred:
    context: cx
  twilio_chat_aht:
    context: cx

custom_cluster:
  driver_node_type_id: m5a.2xlarge
  node_type_id: m5a.2xlarge
  spark_version: 12.2.x-scala2.12
  autoscale:
    max_workers: 3
    min_workers: 1
