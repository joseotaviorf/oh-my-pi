cluster:
  type: emr_7_12_consolidation_xs_general_fleet_cluster
  custom_configurations:
    spark_conf:
      # ts_classified is stored as UTC (see clean-layer metadata); pin the session
      # timezone so CAST(... AS DATE) truncates the same way the IDPLATF-8282
      # reference Trino query's `AT TIME ZONE 'UTC'` does.
      spark.sql.session.timeZone: UTC
